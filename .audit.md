---
name: Arenibus Marketing Audit — SEO, performance, accessibility & code quality
description: Autonómny agent na hĺbkový audit marketingovej webstránky Arenibus. Pokrýva SEO, Core Web Vitals, prístupnosť (WCAG 2.1 AA), kvalitu Next.js/React kódu, bezpečnosť statického exportu, konzistenciu dizajnu, obsah a správnosť deployu. Každý beh končí self-improvement krokom, ktorým sa tento súbor reflektuje.
---

# Arenibus Marketing Audit — SEO, Performance, Accessibility & Quality

## Project Overview

**Arenibus Marketing Website** is a static Next.js 16 marketing site for the Arenibus nephrology information system (MVP phase).

| Layer | Technology | Location |
|-------|------------|----------|
| Framework | Next.js 16 (App Router, static export) | `app/` |
| Language | TypeScript | `*.ts`, `*.tsx` |
| Styling | Tailwind CSS v4 | `app/globals.css` |
| Assets | Static images, SVG favicon | `public/` |
| Build output | Static HTML/JS/CSS | `out/` |
| Hosting | WebSupport SSH (static files) | `deploy.sh`, `deploy.bat` |
| Primary URL | https://arenibus.polascin.net/ | — |

Key pages:

- `app/page.tsx` — single-page marketing landing page (hero, features, demo CTA, contact)
- `app/layout.tsx` — root layout with metadata and theme provider
- `app/globals.css` — design tokens, light/dark theme CSS variables
- `next.config.ts` — static export configuration
- `public/favicon.svg` — branded SVG favicon
- `public/apple-touch-icon.png` — Apple touch icon

---

## 🎯 Agent Goals

Autonomous agent for deep, recurring audits of the Arenibus marketing site. Covers:

1. **SEO discoverability** — metadata, Open Graph, structured data, sitemap, robots, canonical URLs, heading hierarchy, internal links.
2. **Performance** — Core Web Vitals, bundle size, image/font optimization, static export efficiency.
3. **Accessibility (WCAG 2.1 AA)** — color contrast, keyboard navigation, focus indicators, screen-reader semantics, motion preferences.
4. **Code quality** — TypeScript strictness, Tailwind patterns, Next.js idioms, dead code, dependency hygiene.
5. **Security of static export** — hardcoded secrets, unsafe inline scripts, CSP considerations, dependency vulnerabilities.
6. **Content correctness** — Slovak copy accuracy, brand consistency, demo integration, contact details.
7. **Deployment health** — build succeeds, output is valid, deploy script works, live site matches build.

**Every run ends with a self-improvement step**: record new patterns, false positives, and newly discovered project-specific checks back into this file so the next audit is sharper.

---

## 🔍 Primary Tasks

### 1. SEO Audit

SEO is the primary focus of this audit. The marketing site must rank and share correctly.

#### 1.1 Metadata (`app/layout.tsx`, `app/page.tsx`)

- **Title tag:** unique, descriptive, ≤ 60 chars, includes "Arenibus".
  - Current expected: `Arenibus – Nefrologický a dialyzačný informačný systém`
- **Meta description:** unique per page, ≤ 160 chars, actionable.
- **Canonical URL:** present and points to `https://arenibus.polascin.net/`.
- **Language / locale:** `lang="sk"` or `lang="sk-SK"` set on `<html>`.
- **Open Graph:** `og:title`, `og:description`, `og:type`, `og:url`, `og:image`.
  - `og:image` must be an absolute URL (`https://arenibus.polascin.net/...`) with recommended 1200×630 px.
- **Twitter Cards:** `twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`.
- **Robots meta:** no accidental `noindex` in production.
- **Viewport & theme-color:** present.

#### 1.2 Favicon & PWA basics

- `favicon.ico` or `favicon.svg` linked correctly.
- `apple-touch-icon.png` linked via `<link rel="apple-touch-icon" />`.
- Recommended: `manifest.json` with theme/background colors and icons.
- Theme color matches brand (`#0f766e`).

#### 1.3 Heading hierarchy (`app/page.tsx`)

- Exactly one `<h1>` per page.
- Heading levels must not skip (`h1 → h2 → h3`, never `h1 → h3`).
- Feature section headings use semantic heading tags, not styled `<div>`.
- No empty headings.

#### 1.4 Structured data

- Add `application/ld+json` JSON-LD for `SoftwareApplication` or `Organization`.
  - Required fields: `@context`, `@type`, `name`, `description`, `url`, `applicationCategory`, `offers`/`isAccessibleForFree`.
  - Optional but recommended: `logo`, `sameAs`, `provider`.

#### 1.5 Sitemap & robots

- `public/robots.txt` exists and allows all public paths:
  ```
  User-agent: *
  Allow: /
  Sitemap: https://arenibus.polascin.net/sitemap.xml
  ```
- `public/sitemap.xml` exists and lists the canonical homepage URL with `<lastmod>` and `<priority>`.
- **Note:** Next.js static export does not auto-generate sitemap/robots; they must live in `public/`.

#### 1.6 URL & link hygiene — inventory, verify, fix

Every audit run must **inventory**, **verify**, and **fix** all user-facing web links in the same pass. Do not stop at reporting. Scope is clickable HTTP(S) links, `fetch` endpoints, and `mailto:` addresses.

**Out of scope:** Open Graph, Twitter Cards, `canonical`, JSON-LD `url` / `sameAs`, `metadataBase`, schema.org `@context`.

**Inventory sources** (do not maintain a manual URL allowlist — parse the source):

- `href` on `<a>` and `<Link>` in `app/**/*.tsx` (`app/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`, and any future pages).
- `fetch("` URLs (contact form → Formspree).
- `Location` headers in `public/privacy.php` and `public/terms.php`.
- `<loc>` entries in `public/sitemap.xml` (treat `https://arenibus.polascin.net/...` as internal paths).

**Internal paths** (`/`, `/privacy/`, `/terms/`): `next.config.ts` sets `trailingSlash: true`. A directory path without a trailing slash is a defect. After `npm run build`, each path must exist as `out/.../index.html` (or the matching static file for extensioned paths such as `.php`).

**Fragments** (`#main-content`, `#features`, `#demo`, `#contact`, and any new ones): the target page must declare a matching `id`.

**External HTTP(S)** (`https://demo.arenibus.com/`, `https://dataprotection.gov.sk/`, Formspree `https://formspree.io/f/mvzebqzj`): GET with a identifiable User-Agent, follow redirects. Success = 2xx/3xx. Formspree GET may return **405** — that is not a failure. Fail on 404, 5xx, timeout, or DNS error. Prefer HTTPS; plain `http://` on a public link is a defect.

**mailto:** only `arenibus@polascin.net` (general contact) and `arenibus@nephroctor.com` (demo-access requests). Query `subject` / `body` is allowed. Any other address is a defect.

**External `<a>` attributes:** `target="_blank"` and `rel` containing both `noopener` and `noreferrer`.

**Anchor text:** descriptive (no "klikni tu" / "viac").

**Fix in the same run:**

- Update a broken `href` to the current official URL; if a stable 301 target exists, point directly at it rather than leaving a redirect chain.
- Add a missing trailing slash or restore a missing `id`.
- Correct a wrong `mailto:` address.
- Add missing `target` / `rel` on external anchors.
- Findings that cannot be fixed safely (e.g. WAF 403 from a CI IP) go under Known Security / project exceptions with the observed status.

**Automated check:** `npm run check:links` (requires a prior `npm run build` so `out/` exists). Must pass locally and in CI.

#### 1.7 Image SEO

- Logo and icons have meaningful `alt` text.
- Decorative images use empty `alt=""`.
- Images are not rendered as text-only `<svg>` without accessible labels.

---

### 2. Performance Audit

- **Core Web Vitals targets:**
  - LCP ≤ 2.5 s
  - INP ≤ 200 ms
  - CLS ≤ 0.1
  - TTFB ≤ 600 ms
- **Bundle:** run `npm run build` and inspect `out/_next/static/...`.
  - No oversized JS chunks.
  - No duplicate icon libraries.
- **Images:**
  - Use Next.js `<Image>` where possible (note: `next/image` with static export may require `unoptimized: true` or loader config).
  - Provide `width`/`height` to prevent CLS.
  - Use WebP/AVIF where appropriate.
- **Fonts:**
  - Self-host or preconnect to font CDN.
  - Use `font-display: swap`.
- **CSS:**
  - Tailwind v4 `@import "tailwindcss"` is correct.
  - No unused custom CSS bloating the bundle.
- **Third-party scripts:** only essential scripts; load async/defer.

---

### 3. Accessibility (WCAG 2.1 AA)

- **Contrast:**
  - Text on brand teal (`--brand: #0f766e`) must meet 4.5:1 for normal text.
  - `--brand-text` (white in light, dark in dark) on brand backgrounds must meet 4.5:1.
  - Links must be distinguishable from body text.
- **Focus:**
  - All interactive elements have visible focus rings.
  - Focus order follows DOM order.
- **Keyboard:**
  - All buttons and links reachable via Tab.
  - Mobile menu toggle works with Enter/Space.
  - Escape closes mobile menu.
- **Screen readers:**
  - Dark mode toggle has `aria-label`.
  - Mobile menu button has `aria-expanded` and `aria-controls`.
  - SVG icons have `aria-hidden` or meaningful labels.
- **Motion:**
  - Respect `prefers-reduced-motion` for any transitions/animations.
- **Language:**
  - `<html lang="sk">` so screen readers use Slovak pronunciation.

---

### 4. Code Quality Audit

#### 4.1 TypeScript

- `strict: true` enabled in `tsconfig.json`.
- No implicit `any`.
- Components have explicit return types or inferred ReactElement.
- No `// @ts-ignore` or `// @ts-nocheck`.

#### 4.2 Next.js / React

- App Router conventions: default exports for pages, correct metadata API.
- No class components.
- Hooks rules respected (no conditional hooks).
- Client components (`"use client"`) only where necessary (theme toggle, localStorage).
- Server Components used by default.

#### 4.3 Tailwind CSS

- Use arbitrary values sparingly; prefer design tokens from `globals.css`.
- No conflicting Tailwind classes on the same element.
- Dark mode uses `data-theme="dark"` and CSS variables, not `dark:` class variants.
- Verify `--color-*` theme registration matches variables used in JSX.

#### 4.4 Dead / unused code

- No commented-out components or imports.
- No unused public assets.
- No dependencies in `package.json` that are not imported.

#### 4.5 Lint

- Run `npm run lint` and fix all errors/warnings.

---

### 5. Security Audit (Static Export)

- **Secrets:** grep for `password`, `secret`, `api_key`, `token`, `private` in code.
  - Must not find hardcoded credentials.
- **Client-side exposure:** no sensitive logic in client components.
- **CSP readiness:** static export cannot set headers; document any inline scripts and recommend moving them to external files.
- **Dependencies:**
  - `npm audit` should pass or have documented acceptable exceptions.
  - Keep Next.js and React up to date.
- **Links:** external links use `rel="noopener noreferrer"`.
- **Forms:** contact form does not submit to insecure endpoint; if backendless, it must clearly indicate that it is informational or use a safe form service.

---

### 6. Content & Brand Consistency Audit

- **Brand alignment:** colors, logo, and tone match `https://demo.arenibus.com/`.
- **Demo section:**
  - Link to `https://demo.arenibus.com/` is correct and reachable (HTTP 2xx/3xx).
  - Demo passwords must never appear in plaintext; access is provided on request via `arenibus@nephroctor.com`.
- **Contact:** `mailto:arenibus@polascin.net` for general contact; `mailto:arenibus@nephroctor.com` for demo-access requests. No other mailto addresses.
- **Slovak copy:**
  - Correct diacritics and grammar.
  - Consistent terminology (e.g., "nefrologická ambulancia", "informačný systém").
- **Feature list:** reflects actual MVP features documented in the core Arenibus system.
- **Copyright / footer:** matches demo site format.

---

### 7. Build & Deployment Audit

- `npm run build` succeeds with zero errors.
- `next.config.ts` sets `output: 'export'` and correct `distDir` or asset prefix if needed.
- `out/` directory contains `index.html` at the root.
- `deploy.sh` / `deploy.bat`:
  - Build step runs before upload.
  - SSH target path is correct: `/data/8/6/868f981d-e598-4e71-b7f5-246f2e180cef/polascin.net/arenibus`.
  - Backup step does not fail silently.
  - Permissions are set after upload.
- **Live verification:** after deploy, `curl -I https://arenibus.polascin.net/` returns `200 OK`.

---

## 📊 Audit Output Format

For every run, produce a structured report:

```markdown
# Arenibus Marketing Audit Report — <date>

## Summary
- Critical: 0
- High: 0
- Medium: 0
- Low: 0
- Info: 0

## Findings

### <ID>. <Title>
- **Severity:** Critical | High | Medium | Low | Info
- **Category:** SEO | Performance | Accessibility | Code Quality | Security | Content | Deployment
- **File(s):** `<path>`
- **Description:** What is wrong and why it matters.
- **Recommendation:** Specific, actionable fix.

## Fixed in this run
- List of resolved findings.

## Self-improvement notes
- New patterns to add to `.audit.md` for next runs.
```

Severity definitions:

- **Critical:** broken deploy, build failure, hardcoded secret, security vulnerability, site unavailable.
- **High:** missing canonical/OG/sitemap, severe accessibility blocker, major performance regression.
- **Medium:** heading hierarchy issue, contrast near threshold, missing alt text, dead code.
- **Low:** minor copy inconsistency, missing manifest, suboptimal image format.
- **Info:** suggestions and future improvements.

---

## 🛠️ Commands to Run

```bash
# Build & lint
npm run build
npm run lint
npm run check:contrast
npm run check:links   # requires out/ from the build above

# Dependency audit
npm audit

# Optional: run a local server and use Lighthouse
npx serve out
# Then open Chrome DevTools → Lighthouse for mobile/desktop

# Optional: validate sitemap/robots
# curl -s https://arenibus.polascin.net/sitemap.xml
# curl -s https://arenibus.polascin.net/robots.txt
```

---

## 🔄 Self-Improvement Loop

After every audit run:

1. Review false positives and update this file to avoid them next time.
2. Add newly discovered project-specific checks (e.g., a recurring typo, a fragile class pattern, a deployment edge case).
3. Keep the checklist above ordered by business impact: SEO → Performance → Accessibility → Code Quality → Security → Content → Deployment.
4. If a finding repeats across runs, escalate its severity or add an explicit recurring check.

---

## 📊 Audit Reports

### 2026-07-25 — Add development-phase notice

#### Summary
- Critical: 0
- High: 0
- Medium: 1
- Low: 0
- Info: 0

#### Findings

##### 1. Missing development-phase disclaimer
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** The marketing page did not visibly state that the information system is still in advanced development. This could create unrealistic expectations for clinical/production use.
- **Recommendation:** Add a clear, prominent notice near the main headline and in the demo section that Arenibus is still in advanced development.
- **Fixed:** Added a badge "Stále vo fáze pokročilého vývoja" under the hero `<h1>` and updated the demo section text to say "Funkčné demo nefrologickej ambulancie v pokročilej fáze vývoja."

#### Fixed in this run
- Added development-phase badge in hero section.
- Updated demo section wording to reflect advanced development status.

#### Self-improvement notes
- Keep the development-phase badge visible until the system leaves MVP/advanced-development status.
- Revisit the disclaimer before any production-ready release.

---

### 2026-07-25 — Sync with demo and local Arenibus project

#### Summary
- Critical: 0
- High: 0
- Medium: 1
- Low: 0
- Info: 0

#### Findings

##### 1. Demo credentials on the marketing site were incomplete
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** The demo section showed only usernames (`demo-lekar / demo-sestra`) but omitted the required passwords and the OIDC/Keycloak login flow. This could prevent visitors from accessing the demo.
- **Recommendation:** Update the demo helper text to include the full credentials (`[redacted]` / `[redacted]`) and mention Keycloak/OIDC login.
- **Fixed:** Updated to: "Demo beží na https://demo.arenibus.com/ — prihlásenie cez Keycloak (OIDC). Kontá: demo-lekar / [redacted] alebo demo-sestra / [redacted]."

#### Cross-checked against local Arenibus project
- **Phase:** Fáza 1 — MVP nefrologická ambulancia, evidenčné jadro funkčné end-to-end.
- **Implemented modules:** Patients, Encounters, Dialysis (základ Fázy 2), Codebooks, Identity (OIDC/Keycloak), Audit, Billing (základ — dávky 751a/799a ešte nie sú export-ready).
- **Marketing feature cards already align with implemented MVP features:** Správa Pacientov, Evidencia Návštev, Dialyzačný Predpis, Objednávanie Termínov, Audit a Bezpečnosť, Laboratórne Výsledky.
- **No new feature cards added** because Billing/Fakturácia je stále v základnom stave (žltý) a dialýza je explicitne označená ako základ Fázy 2.

#### Fixed in this run
- Updated demo credentials and login method in `app/page.tsx`.
- Verified marketing copy against `README.md` and `docs/STATUS.md` of the local Arenibus project.

#### Self-improvement notes
- Add a recurring check that demo credentials match the latest `README.md` of the main Arenibus repo.
- When Billing/Fakturácia becomes fully export-ready, consider adding a dedicated feature card.

---

### 2026-07-24 — Automated deep audit

#### Summary
- Critical: 0
- High: 0
- Medium: 0
- Low: 0
- Info: 1

#### Findings

##### 1. Meta description exceeded 160 characters
- **Severity:** Medium
- **Category:** SEO
- **File(s):** `app/layout.tsx`
- **Description:** The `<meta name="description">` content was 197 characters long, which may be truncated in search-engine snippets.
- **Recommendation:** Shorten the description to ≤ 160 characters while keeping the key value proposition.
- **Fixed:** Shortened to 102 characters: "Moderný .NET systém pre nefrologickú ambulanciu a dialyzačné stredisko, integrovaný s ezdravotníctvom."

##### 2. ARIA labels on interactive controls used English copy
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** The dark-mode toggle used `aria-label="Toggle dark mode"` and the mobile menu used `aria-label="Menu"`, while the rest of the interface is in Slovak.
- **Recommendation:** Localize all screen-reader labels to Slovak.
- **Fixed:** Updated to `aria-label="Prepnúť tmavý režim"` and `aria-label="Otvoriť alebo zavrieť menu"`.

##### 3. Sitemap `lastmod` was stale
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** The `<lastmod>` value was `2026-07-22`, two days behind the current deploy date.
- **Recommendation:** Update `<lastmod>` on each deploy or automate it via the deploy pipeline.
- **Fixed:** Updated to `2026-07-24`.

##### 4. Known transitive dependency vulnerabilities
- **Severity:** Info
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit` reports 3 high-severity issues in Next.js 16.2.11 transitive dependencies (`postcss`, `sharp`). The suggested fix downgrades Next.js to 9.3.3, which is a breaking change.
- **Recommendation:** Monitor Next.js releases and upgrade when a patched 16.x version is available. This is already documented as a known exception.

#### Fixed in this run
- Shortened meta description in `app/layout.tsx`.
- Localized dark-mode and mobile-menu `aria-label` values in `app/page.tsx`.
- Updated `public/sitemap.xml` `<lastmod>`.
- Verified build, lint, contrast ratios, heading hierarchy, alt text, canonical/OG/Twitter tags, JSON-LD, robots.txt, and manifest.

#### Self-improvement notes
- Add a recurring check that the meta description stays ≤ 160 characters.
- Add a recurring check that all `aria-label` text on the page is in Slovak.
- Consider automating `<lastmod>` in `sitemap.xml` via the CI/CD workflow.
- Contrast ratios for the current palette are well above WCAG 2.1 AA thresholds; no extra tooling needed unless colors change.

---

### 2026-07-28 — Automated deep audit

#### Summary
- Critical: 0
- High: 0
- Medium: 0
- Low: 1
- Info: 1

#### Findings

##### 1. Sitemap `lastmod` was stale
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** The sitemap listed `2026-07-24`, which was four days behind the current audit and release date.
- **Recommendation:** Refresh `<lastmod>` whenever a production release changes public content, or automate it in CI/CD.
- **Fixed:** Updated `<lastmod>` to `2026-07-28`.

##### 2. Known transitive dependency vulnerabilities remain
- **Severity:** Info
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit --omit=dev` reports three high-severity transitive vulnerabilities through Next.js 16.2.11 (`postcss` and `sharp`). Next.js 16.2.12 was published on 2026-07-25 but is not yet seven days old; the only offered automated remediation downgrades Next.js to 9.3.3, which is incompatible.
- **Recommendation:** After 2026-08-01, test upgrading to Next.js 16.2.12 through the normal dependency-update process; do not use the breaking `npm audit fix --force` suggestion.

#### Fixed in this run
- Refreshed the sitemap `lastmod` value.
- Verified lint, production build, live homepage, robots.txt, sitemap.xml, demo availability, metadata, structured data, heading hierarchy, form labels, focus treatment, reduced-motion handling, external-link safety, and secret scan.

#### Self-improvement notes
- Treat an outdated sitemap `<lastmod>` as a release-hygiene finding and refresh it whenever public content changes.
- The current production bundle has no individual CSS or JavaScript asset larger than 228 KB uncompressed; re-evaluate if a future change exceeds this baseline materially.

---

### 2026-08-15 — Automated deep audit

#### Summary
- Critical: 0
- High: 0
- Medium: 0
- Low: 1
- Info: 1

#### Findings

##### 1. Sitemap `lastmod` was stale
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** The sitemap listed `2026-07-28`, 18 days behind the current audit date.
- **Recommendation:** Refresh `<lastmod>` on each release; consider automating it in the deploy workflow.
- **Fixed:** Updated `<lastmod>` to `2026-08-15`.

##### 2. `nanoid` had a fixable high-severity vulnerability
- **Severity:** High (fixed, non-breaking)
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit` reported `nanoid <3.3.18` (transitive via `postcss`) is vulnerable to indefinite loops with a zero size generator (GHSA-2v37-7h3g-55p8).
- **Recommendation:** Run `npm audit fix` to update within the existing semver range.
- **Fixed:** Ran `npm audit fix`, which updated `nanoid` to `3.3.18` (or later) without touching `package.json` ranges. Verified with `npm run lint` and `npm run build`.

##### 3. Known transitive dependency vulnerabilities remain (postcss/sharp via Next.js)
- **Severity:** Info
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit --omit=dev` still reports high-severity transitive vulnerabilities in `postcss` and `sharp` through Next.js 16.2.11. The only remediation path upgrades Next.js to `16.3.1`, published 2026-08-13 — only 2 days before this audit, short of the project's 7-day vetting window.
- **Recommendation:** Re-evaluate upgrading to Next.js 16.3.1 (or newer) on or after 2026-08-20, then run the full audit and regression checks before adopting it.

#### Fixed in this run
- Updated `public/sitemap.xml` `<lastmod>`.
- Ran `npm audit fix` to resolve the `nanoid` vulnerability without breaking changes.
- Verified lint, production build, `npm audit` (remaining findings documented above), live homepage, sitemap.xml, demo availability, and page content/copy.

#### Self-improvement notes
- Track Next.js release dates before upgrading; do not adopt a release younger than 7 days per project policy.
- `npm audit fix` (without `--force`) is safe to run each audit round to pick up in-range transitive patches; only `--force` requires manual review.
- Sitemap `<lastmod>` drifts noticeably between infrequent audit runs; consider wiring it to the deploy workflow's date at build time.

---

### 2026-08-28 — Automated deep audit

#### Summary
- Critical: 1
- High: 3
- Medium: 5
- Low: 8
- Info: 3

#### Findings

##### 1. `deploy.sh` could wipe the wrong directory
- **Severity:** Critical
- **Category:** Deployment
- **File(s):** `deploy.sh`
- **Description:** The remote block ran `cd $DEPLOY_DIR` with no failure guard and then `rm -rf *`. If the deploy directory were ever renamed, unmounted or unreachable, `cd` would fail, the script would continue in the SSH login home directory, and `rm -rf *` would delete the home contents. The GitHub Actions workflow already had `cd ... || exit 1`; the manual fallback script did not.
- **Recommendation:** Guard the `cd`, add `set -euo pipefail`, and replace the bare glob with a bounded `find`.
- **Fixed:** Rewrote `deploy.sh` with `set -euo pipefail`, an explicit `if ! cd "$DEPLOY_DIR"; then ... exit 1; fi` guard, quoted variables, and `find . -mindepth 1 -maxdepth 1 -exec rm -rf -- {} +`.

##### 2. Hero LCP image was a 4.73 MB PNG
- **Severity:** High
- **Category:** Performance
- **File(s):** `public/ArenibusLogo.png`, `app/page.tsx`
- **Description:** A single 2048x2048, 4.73 MB PNG served as the hero image (rendered at 384 CSS px, marked `priority`), the nav logo (40 px), the Open Graph image, the Apple touch icon and a manifest icon. With `images.unoptimized: true` (required by `output: 'export'`), Next.js serves the file byte-for-byte, so every visitor downloaded 4.73 MB for the LCP element. It accounted for 4.84 MB of the 5.9 MB build output — 82 % of the payload.
- **Recommendation:** Generate purpose-sized derivatives; the source is photographic/illustrative, so WebP compresses it dramatically.
- **Fixed:** Generated `logo-768.webp` (61 KB, hero) and `logo-96.webp` (3.2 KB, nav) with sharp, and repointed both `<Image>` elements. Hero image payload dropped 98.7 % (4842 KB to 61 KB); total `out/` dropped from 5.9 MB to 1.37 MB (− 77 %). After rebasing onto the privacy/terms pages added in 54064b4, the full six-route output is 1.53 MB.

##### 3. Deployment backup was destroyed immediately after creation
- **Severity:** High
- **Category:** Deployment
- **File(s):** `.github/workflows/deploy.yml`, `deploy.sh`
- **Description:** Both deploy paths ran `tar -czf "backup_$(date ...).tar.gz" .` **inside** `$DEPLOY_DIR` and then wiped the directory. The cleanup glob matched the archive that had just been written, so every deployment silently destroyed its own rollback point. The archive also tried to include itself while being written.
- **Recommendation:** Write the archive outside the deploy directory and retain a bounded history.
- **Fixed:** Backups now go to `$HOME/arenibus-backups/backup_<timestamp>.tar.gz` via `tar -czf "$BACKUP_FILE" -C "$DEPLOY_DIR" .`, keeping the five most recent archives and pruning older ones. Applied to both the workflow and `deploy.sh`. The first deploy confirmed the archive is now written to `/data/8/6/<uuid>/arenibus-backups/`, outside the wiped tree.
- **Follow-up:** The first run reported `Backup size: 512`, which read as a suspiciously empty archive. Rather than trust or dismiss it, the step was made to prove its own output: it counts entries under `$DEPLOY_DIR` before archiving, re-reads the archive with `tar -tzf`, prints exact byte and entry counts, and aborts *before* the cleanup if the archive holds fewer entries than the source. The next deploy reported `Backup: 621955 bytes, 60 entries` against 59 source entries (the extra one is the archive root `.`), confirming the backup was genuine all along and that `du -h` had simply been reporting 512-byte allocation blocks. The verification stays in place so the question can never be ambiguous again.

##### 4. Three high-severity dependency vulnerabilities
- **Severity:** High
- **Category:** Security
- **File(s):** `package.json`, `package-lock.json`
- **Description:** `npm audit` reported three high-severity advisories reachable through Next.js 16.2.11: four `postcss` issues (XSS via unescaped `</style>`, plus three `sourceMappingURL` arbitrary-file-read / path-traversal advisories) and `sharp <0.35.0` inheriting four libvips CVEs.
- **Recommendation:** Upgrade Next.js rather than using the breaking `npm audit fix --force` suggestion (which proposed `next@9.3.3` in an earlier run).
- **Fixed:** Upgraded to `next@16.3.1` and `eslint-config-next@16.3.1` (pinned exactly). 16.3.1 ships `postcss@8.5.23` and `sharp@^0.35.3`, clearing all three advisories. Chosen over the newer 16.3.2 (5 days old) and 16.3.3 (1 day old) because only 16.3.1 (13 days old) satisfied the project's 7-day vetting window. `npm audit` now reports **0 vulnerabilities**.

##### 5. Open Graph image had the wrong aspect ratio and size
- **Severity:** Medium
- **Category:** SEO
- **File(s):** `app/layout.tsx`, `public/og-image.png`
- **Description:** `og:image` pointed at the square 2048x2048 logo while declaring `width: 1200, height: 630`. Social platforms trust the declared dimensions, so link previews were letterboxed or cropped, and the 4.73 MB payload exceeds several platforms' fetch limits.
- **Recommendation:** Ship a real 1200x630 card and keep the declared dimensions truthful.
- **Fixed:** Composited a dedicated `og-image.png` (1200x630, 139 KB) — the logo centred on the background colour sampled from its own border (`#1e374a`) for a seamless card. `og:image` and `twitter:image` now point to it; verified the absolute URL is emitted in `out/index.html`.

##### 6. Development badge failed WCAG 2.1 AA contrast (light mode)
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/globals.css`
- **Description:** The "Stále vo fáze pokročilého vývoja" badge renders `text-sm` in `--warn` on `--warn-soft`. Measured contrast was **4.31:1**, below the 4.5:1 AA threshold for normal text. Earlier rounds recorded the palette as "well above WCAG 2.1 AA thresholds" without ever measuring it.
- **Recommendation:** Darken `--warn` minimally, keeping the amber hue.
- **Fixed:** `--warn: #b45309` to `#ae5009`, giving **4.57:1**.

##### 7. Footer secondary text failed WCAG 2.1 AA contrast (dark mode)
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/globals.css`
- **Description:** The footer renders `text-sm text-muted` on `bg-surface-3`. In dark mode `--muted` on `--surface-3` measured **4.43:1**, below the 4.5:1 AA threshold.
- **Recommendation:** Lighten `--muted` minimally in both dark blocks.
- **Fixed:** `--muted: #8fa8a0` to `#92aba3` in the `[data-theme="dark"]` block *and* the `prefers-color-scheme: dark` block, giving **4.59:1**.

##### 8. Horizontal overflow on narrow viewports
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** The hero logo wrapper used a fixed `w-96 h-96` (384 px). On a 360 px viewport the section's `px-4` padding leaves 328 px, so the wrapper forced the document 12 px wider than the viewport, producing horizontal scrolling on common phone widths — a Google mobile-friendliness signal.
- **Recommendation:** Let the wrapper shrink below its maximum size.
- **Fixed:** `w-96 h-96` to `w-full max-w-96 aspect-square`. Measured in-browser at a 360 px viewport: `scrollWidth` 372 to 360, overflowing elements 8 to 0. The existing `sizes="(max-width: 768px) 100vw, 384px"` is now accurate.

##### 9. Apple touch icon served the full-size logo
- **Severity:** Medium
- **Category:** Performance / SEO
- **File(s):** `app/layout.tsx`, `public/apple-touch-icon.png`
- **Description:** `icons.apple` pointed at the 4.73 MB logo, so iOS downloaded the whole file to render a 180x180 home-screen icon.
- **Fixed:** Added `apple-touch-icon.png` (180x180, 21 KB) and repointed the metadata.

##### 10. Manifest declared incorrect icon dimensions
- **Severity:** Low
- **Category:** SEO / PWA
- **File(s):** `public/manifest.json`
- **Description:** The manifest declared `/ArenibusLogo.png` as `sizes: "512x512"` while the file was 2048x2048, and listed `/favicon.ico` as a PWA icon. Mismatched `sizes` makes installability checks unreliable.
- **Fixed:** Replaced with truthful `icon-192.png` (192x192) and `icon-512.png` (512x512) entries plus `purpose: "any"`, and added `lang: "sk-SK"` and `scope: "/"`.

##### 11. Dead assets shipped in `public/`
- **Severity:** Low
- **Category:** Code Quality
- **File(s):** `public/favicon.ico`, `public/ArenibusLogo.png`
- **Description:** `public/favicon.ico` (168 KB) was shadowed by the App Router file convention `app/favicon.ico` (25 KB) — confirmed because `out/favicon.ico` always matched the `app/` copy byte-for-byte — so it was never served. After the image work, `ArenibusLogo.png` had no remaining references.
- **Fixed:** Removed both. The 2048x2048 master remains recoverable from git history (`git show c0af678:public/ArenibusLogo.png`). Verified no dangling references in `out/`, that `/ArenibusLogo.png` now 404s, and that `/favicon.ico` still serves the `app/` icon.

##### 12. Theme flash on load for users overriding the system preference
- **Severity:** Low
- **Category:** Accessibility / Performance
- **File(s):** `app/layout.tsx`
- **Description:** `data-theme` was only applied in a `useEffect` after hydration. The CSS `prefers-color-scheme` fallback covered users whose stored choice matched their system, but anyone who had toggled against it saw a flash of the wrong theme on every load.
- **Fixed independently on `main`.** This round produced its own blocking `themeInit` script, but commit 54064b4 had already landed an equivalent bootstrap (plus `suppressHydrationWarning`). During the rebase the duplicate was removed and the `main` version kept: it sets `data-theme` only when `localStorage` holds a choice, leaving the CSS `prefers-color-scheme` block to serve users who never toggled. Recorded because the finding was reached independently and the surviving single script is the intended end state.

##### 13. Mobile menu stayed open after choosing a link
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** Selecting a section link in the mobile menu scrolled the page but left the menu overlaying the content.
- **Fixed:** Each mobile link now calls `setMobileMenuOpen(false)`.

##### 14. Form feedback live region was created on demand
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** The success/error messages mounted their `role="status"` / `role="alert"` element at the same moment as their text. Screen readers can miss announcements when the live region and its content appear in the same update.
- **Fixed:** Replaced with a single persistent `role="status" aria-live="polite"` element that is `sr-only` while idle and gains the success/error styling when a message appears.

##### 15. Sitemap `lastmod` was stale
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** Listed `2026-08-15`, 13 days behind this release.
- **Fixed:** Updated to `2026-08-28`.

##### 16. `.trunk/` polluted the working tree
- **Severity:** Low
- **Category:** Code Quality
- **File(s):** `.gitignore`
- **Description:** An untracked `.trunk/` directory from a local trunk.io run appeared in every `git status`.
- **Fixed:** Added `.trunk/` to `.gitignore`. Note this also ignores `trunk.yaml` and `configs/`, which trunk.io normally expects to be committed — deliberately left uncommitted, because adopting trunk.io as project tooling is a maintainer decision, not an audit change.

##### 17. Deploy permission handling was imprecise
- **Severity:** Low
- **Category:** Deployment
- **File(s):** `.github/workflows/deploy.yml`, `deploy.sh`
- **Description:** `chmod -R 755 .` followed by `chmod -R 644 *.html *.json *.js *.css` relied on top-level globs and would have stripped the execute bit from directories had any glob matched one.
- **Fixed:** Replaced with `find . -type d -exec chmod 755 {} +` and `find . -type f -exec chmod 644 {} +`.

##### 18. `"Inter Variable"` is referenced but never loaded
- **Severity:** Info
- **Category:** Performance / Content
- **File(s):** `app/globals.css`
- **Description:** The body font stack starts with `"Inter Variable"`, but there is no `@font-face`, `next/font` usage or CDN link anywhere. Visitors who happen to have Inter installed locally see Inter; everyone else falls back to Segoe UI / `system-ui`, so typography differs between visitors.
- **Recommendation:** Either self-host Inter via `next/font/local` for consistency (costs a font download) or drop the name for a purely system-font stack (keeps the current zero-download behaviour). **Deliberately not changed** — it alters the site's typography and is a design decision for the maintainer.

##### 19. Local `npm run build` is blocked by Windows Application Control
- **Severity:** Info
- **Category:** Deployment
- **Description:** On this workstation the default Turbopack build fails: a WDAC policy blocks `@next/swc-win32-x64-msvc/next-swc.win32-x64-msvc.node`, Next.js falls back to WASM bindings, and Turbopack refuses to run without native bindings. CI is unaffected (ubuntu-latest has working native bindings).
- **Recommendation:** Use `npx next build --webpack` for local verification. `npm run build` was **left on Turbopack** so CI keeps the faster path; this is a workstation policy issue, not a repository defect.

##### 20. CSP readiness unchanged by the new inline script
- **Severity:** Info
- **Category:** Security
- **File(s):** `app/layout.tsx`
- **Description:** `out/index.html` contains three bare inline `<script>` blocks: the new `themeInit` plus two Next.js RSC payload scripts (`self.__next_f.push(...)`), alongside the `application/ld+json` block. A future CSP would need hashes or `'unsafe-inline'` regardless of the theme script, so it does not change the existing posture. Static export cannot set headers, so any CSP must come from the hosting layer.

#### Fixed in this run
- Rewrote `deploy.sh` safely (`set -euo pipefail`, guarded `cd`, bounded `find`-based cleanup, quoted variables, post-deploy HTTP verification).
- Moved deployment backups outside the wiped directory with 5-archive retention, in both the workflow and `deploy.sh`.
- Replaced the 4.73 MB logo with purpose-built assets: `logo-768.webp`, `logo-96.webp`, `og-image.png`, `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`.
- Upgraded to Next.js 16.3.1 — `npm audit` now reports 0 vulnerabilities.
- Fixed two measured WCAG 2.1 AA contrast failures and the mobile horizontal overflow.
- Added mobile-menu close-on-select and a persistent form live region. (The pre-paint theme script was already fixed on `main`; the duplicate introduced this round was dropped during the rebase.)
- Corrected `manifest.json`, refreshed `sitemap.xml`, removed two dead assets, ignored `.trunk/`.
- Added `scripts/check-contrast.mjs` plus `npm run check:contrast`, wired into CI as a required step.

#### Verification performed
- `npm run lint` clean; `npx next build --webpack` succeeds for all six routes (`/`, `/privacy`, `/terms`, `/_not-found`); `npm audit` reports 0 vulnerabilities. Every check was re-run after rebasing onto 54064b4.
- `npm run check:contrast`: all 26 token pairs pass in both themes, plus dark-palette parity between the two dark CSS blocks.
- Served `out/` locally and confirmed HTTP 200 and correct `Content-Type` for every asset, and that `/ArenibusLogo.png` now 404s.
- Structural checks on the generated HTML: exactly one `<h1>`, no skipped heading levels, all four fragment anchors resolve, external links carry `rel="noopener noreferrer"`, both images have `alt`, all 9 SVGs are `aria-hidden`, all three form fields have `<label for>`, all `aria-label`s are Slovak.
- Headless-Chrome renders at 1280 px (light and dark) and 360 px; in-browser layout measurement confirmed zero overflowing elements after the fix.
- Live checks: `https://arenibus.polascin.net/`, `/robots.txt`, `/sitemap.xml` and `https://demo.arenibus.com/` all returned HTTP 200.

#### Self-improvement notes
- **Measure, do not assert.** Two real AA contrast failures (4.31:1 and 4.43:1) survived several rounds because a previous run recorded the palette as "well above WCAG thresholds" without measuring. `npm run check:contrast` now runs in CI so this cannot regress silently.
- **Do not trust headless screenshots for layout facts.** The 360 px screenshot appeared to show nav controls pushed off-screen and content severely clipped; direct `getBoundingClientRect` measurement showed the real defect was a 12 px overflow. Use screenshots to spot *candidates*, then confirm geometry by measuring inside the page.
- **Audit asset weight, not just asset presence.** Earlier rounds confirmed that `og:image`, the apple icon and manifest icons *existed*, but never checked what they weighed or whether declared dimensions matched the file. One 4.73 MB file was doing five different jobs badly.
- **Purged Tailwind classes invalidate before/after DOM experiments.** Re-applying an old utility class (`w-96`) in the browser did nothing, because the class no longer exists in the built CSS. Reproduce prior geometry with inline styles instead.
- **A backup is not fixed until it is verified.** Moving the archive out of the wiped directory was necessary but not sufficient, and a `du -h` figure of "512" was not evidence either way. Deploy steps that guard against data loss should assert their own output in unambiguous units (bytes and entry counts), not just exit 0.
- **Check the manual deploy path, not just CI.**
- Next.js 16.3.2 (2026-08-21) and 16.3.3 (2026-08-25) were available but below the 7-day vetting window at audit time; re-evaluate next run.
- Demo credentials are no longer published on the site at all: 54064b4 replaced them with an e-mail request flow, and the standing check is now that they must never appear in plain text in this public repository. The earlier "cross-check against the main repo README" check is retired.
- Two branches independently produced a theme-flash fix in the same release. Before adding a bootstrap script to `<head>`, check whether `main` already ships one.

---

### Run 4 — 2026-08-28 (Comprehensive Audit & Demo Alignment)

#### Summary of findings
- **Critical (0)**: None.
- **High (0)**: None.
- **Medium (Fixed)**: Broken image references in `app/privacy/page.tsx` and `app/terms/page.tsx` referencing deleted `/ArenibusLogo.png` were fixed to use `/og-image.png` for metadata and `/logo-96.webp` for navigation.
- **Low (Fixed)**: `app/layout.tsx` JSON-LD description and `public/manifest.json` updated to match the expanded scope (hemodialysis & peritoneal dialysis) and system title *Arenibus – Nefrologický a dialyzačný informačný systém*.
- **Content Alignment (Fixed)**: Expanded features list to 8 core modules and integrated interactive demo credentials with one-click copy and nightly 03:00 reset schedule information.

#### Verification performed
- `npm run lint`: clean (0 errors, 0 warnings).
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes.
- `npm run build`: successful static export for all routes (`/`, `/privacy`, `/terms`, `/_not-found`).
- `npm audit`: 0 vulnerabilities found.
- Grep scan for secrets: 0 exposed secrets in codebase.
- Live verification: `https://arenibus.polascin.net/` returns HTTP 200 with new metadata and content.

#### Self-improvement notes
- Ensure sub-pages (`privacy`, `terms`) image references are verified during asset reorganizations to prevent broken metadata images.
- Keep `manifest.json` and JSON-LD structured data synchronized whenever root metadata description changes.

---

### Run 5 — 2026-08-28 (Repeated comprehensive audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 3 (fixed)
- Info: 0 remaining actionable

#### Findings

##### 1. Next.js 16.3.1 is in the affected range of GHSA-p293-qw3h-jr36
- **Severity:** High
- **Category:** Security
- **File(s):** `package.json`
- **Description:** Next.js 16.3.3 (2026-08-25) patches two critical RCEs, including unauthenticated RCE on Windows-hosted servers (`>= 16.0 < 16.3.3`). Production is a Linux static export, but local `next dev` / `next build` runs on a Windows workstation. The 7-day feature-vetting window does not apply to critical security patches.
- **Recommendation:** Upgrade `next` and `eslint-config-next` to 16.3.3.
- **Fixed:** Upgraded to `next@16.3.3` and `eslint-config-next@16.3.3`. `npm audit` reports 0 vulnerabilities.

##### 2. Terms meta description exceeded 160 characters
- **Severity:** Medium
- **Category:** SEO
- **File(s):** `app/terms/page.tsx`
- **Description:** Generated `out/terms/index.html` description was 165 characters. The homepage-only length check missed it.
- **Recommendation:** Shorten the terms description and measure all routes in `out/`.
- **Fixed:** Description is now 121 characters and shared across title/OG/Twitter fields.

##### 3. Features heading drifted from the standing check
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Heading had been shortened to `Funkcie systému Arenibus v aktuálnej verzii (v0.16.17)`, dropping the required MVP expansion lines.
- **Recommendation:** Restore the standing-check wording.
- **Fixed:** Restored the full heading plus `(MVP = Minimum Viable Product)` and `(Minimálny životaschopný produkt)` on separate lines.

##### 4. Demo mailto hover used white text on dark brand teal
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** `hover:text-white` on the demo band would paint `#ffffff` on dark-mode `--brand` `#2dd4bf`, which is not an AA pair and was not in `check:contrast` PAIRS.
- **Recommendation:** Keep `text-brand-text` on hover; do not introduce a new white-on-teal combination.
- **Fixed:** Hover now only adjusts underline offset.

##### 5. Contact form errors used `role="status"`
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** Failures must be announced assertively (`role="alert"`), not as polite status.
- **Recommendation:** Switch role and `aria-live` based on `formStatus`.
- **Fixed:** Errors use `role="alert"` / `aria-live="assertive"`; success stays `status` / `polite`.

##### 6. Sitemap `<lastmod>` stale on /privacy/ and /terms/
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** Subpage dates were still 2026-08-25 after 2026-08-28 content changes.
- **Recommendation:** Refresh `<lastmod>` on every URL in a release, not only the homepage.
- **Fixed:** All three `<loc>` entries now `2026-08-28`.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.3 static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials in source.
- Generated HTML: homepage title 54 / description 139; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; no asset over 200 KB.

#### Self-improvement notes
- Measure meta description length in **every** generated route (`out/index.html`, `out/privacy/index.html`, `out/terms/index.html`), not only the homepage.
- Critical Next.js security releases skip the 7-day feature-vetting window. Feature/minor upgrades still wait 7 days.
- Never use `text-white` (or `hover:text-white`) on the demo brand band; dark `--brand` `#2dd4bf` fails AA against white. Use `text-brand-text`. Add a PAIRS row whenever a new foreground/background combination is introduced — or do not introduce it.
- Refresh `public/sitemap.xml` `<lastmod>` on **all** `<loc>` entries in a release, not only `/`.
- The features-section heading is a standing check; do not silently shorten it when adding a version badge (version already lives in the hero/demo badges).

---

### Run 6 — 2026-09-03 (Demo alignment v0.17.37 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 2 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ (bundle timestamp 2026-09-03 10:41 UTC) identifies as **Arenibus v0.17.37**. The marketing hero badge and demo band still said **v0.16.17**, last aligned on 2026-08-28.
- **Recommendation:** Keep a single `DEMO_VERSION` constant and refresh it whenever the demo footer version changes.
- **Fixed:** Both badges now read `v0.17.37` via `DEMO_VERSION`.

##### 2. Implemented billing / insurance reporting was missing from the feature list
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `app/layout.tsx`
- **Description:** Demo navigation now includes **Vykazovanie** (`/billing`) with dávky 751a (ambulancia and dialyzačný stacionár), 799a, eFaktúra UBL 2.1, cenník, and insurance notifications. Run 5 deferred this card because export was not ready. The demo landing copy already leads with „registre podľa slovenskej legislatívy a vykazovanie poisťovniam“.
- **Recommendation:** Add a dedicated feature card and mention vykazovanie in the hero and meta description.
- **Fixed:** Added card **Vykazovanie poisťovniam**. Hero, JSON-LD, Open Graph, Twitter, and `manifest.json` now mention registre and vykazovanie. Homepage meta description is 137 characters.

##### 3. Dialysis operations module was missing from the feature list
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Demo ships `/operations` (**Prevádzka**): shifts, rooms, weekly staffing roster, machine register, and the register of epidemiologically significant facts. HD/PD cards did not cover this workplace layer.
- **Recommendation:** Add a **Prevádzka dialýzy** card and tighten HD/PD copy (monitors, vascular-access register, CAPD/APD).
- **Fixed:** New card plus updated HD, PD, statistics, and audit cards (retention deadlines).

##### 4. `fast-uri` had a fixable high-severity advisory
- **Severity:** High (fixed, non-breaking)
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit` reported four high-severity issues in `fast-uri` 3.1.5 (transitive via `serve` → `ajv`): host confusion and SSRF via malformed URI normalisation (GHSA-5jgf-p345-68v8, GHSA-f65p-4m7j-42xc, GHSA-fph4-wmhf-6fwf, GHSA-jqff-g426-hqxp).
- **Recommendation:** `npm audit fix` without `--force`.
- **Fixed:** `fast-uri` is now 3.1.7. `npm audit` reports 0 vulnerabilities. `serve` is a devDependency used only by `npm start`.

##### 5. Sitemap `<lastmod>` was stale on every URL
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-08-28.
- **Fixed:** All three now `2026-09-03`.

##### 6. Footer copyright year range drifted from the demo
- **Severity:** Low
- **Category:** Content
- **File(s):** `app/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`
- **Description:** Demo footer uses `© 2024–2026`; marketing pages showed `© 2026` only.
- **Fixed:** All three footers now `Arenibus © 2024–2026 Ľubomír Polaščín`.

##### 7. Next.js 16.3.4 is a non-security follow-up inside the 7-day window
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** `next@16.3.4` (2026-08-31) re-enables AVIF image optimisation and backports three bug fixes. It is not a security release. This site uses `images.unoptimized: true` with `output: 'export'`, so AVIF optimisation does not apply. 16.3.4 is three days old as of 2026-09-03.
- **Recommendation:** Re-evaluate on or after 2026-09-07. Do not skip the 7-day window for this release.

##### 8. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1mkbuudhndal5.js` is 228 922 bytes — the same homepage client-bundle baseline recorded on 2026-08-28. No new client libraries were added. Splitting `app/page.tsx` would be a design change, not a defect.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.3 static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit`: 0 vulnerabilities after the `fast-uri` bump.
- Secret scan: no hardcoded credentials in source.
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; feature cards are `<h3>`; ten feature modules present; version `v0.17.37` in hero and demo band.
- Demo cross-check: landing copy, nav modules (`/billing`, `/operations`, `/reports`, `/dialysis`, `/peritoneal`, `/audit`, `/retention`), and footer version `0.17.37` taken from https://demo.arenibus.com/ assets dated 2026-09-03.

#### Self-improvement notes
- Treat the demo footer version (`Arenibus vX.Y.Z` on https://demo.arenibus.com/) as the source of truth. Store it in one `DEMO_VERSION` constant and fail the audit if the marketing badges disagree.
- Demo **nav labels** (not just README notes) decide which feature cards belong on the marketing page. When `/billing` is in the nav with dávky 751a/799a and eFaktúra download, Vykazovanie is no longer „základ Fázy 2“.
- Fold admin-only screens (Poskytovateľ, Používatelia, Monitory, Cenník, Uchovávanie) into related public cards rather than giving each a tile.
- Keep footer copyright year range in lockstep with the demo (`© 2024–2026`).
- `npm audit fix` without `--force` remains safe each round; this run only moved `fast-uri` 3.1.5 → 3.1.7.
- Next.js 16.3.4 is a non-security follow-up from 2026-08-31; re-evaluate on or after 2026-09-07.

---

### Run 7 — 2026-09-04 (Demo alignment v0.17.41 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-04 00:29 UTC (`index-BMiShhtU.js`). The bundle constant and login/sidebar footer identify as **Arenibus v0.17.41**. Marketing badges still said **v0.17.37** from Run 6 (2026-09-03).
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo footer version changes.
- **Fixed:** Both badges now read `v0.17.41` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing modules shipped in v0.17.41
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). Patient-card sections and the dashboard now expose dohoda o ZS, očkovania, posudky, DPN, výpis dokumentácie, overenie poistného vzťahu (ÚDZS), vzdialená starostlivosť, žiadanky na SVLZ, reklamácie dávok, and personálne očkovania/kvalifikácie. The ten public cards still named the older subset.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, Laboratórium, Moje zoznamy, Vykazovanie, and Prevádzka. PD, Kalendár, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-03 after a content release.
- **Fixed:** All three now `2026-09-04`.

##### 4. Next.js 16.3.4 remains inside the 7-day feature-vetting window
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.4` (2026-08-31). No newer 16.3.x security release. 16.3.4 is four days old as of 2026-09-04. `npm audit` reports 0 vulnerabilities on `next@16.3.3`.
- **Recommendation:** Re-evaluate on or after 2026-09-07. Do not skip the window for this non-security follow-up.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1mkbuudhndal5.js` is 228 922 bytes — unchanged from Run 6. No new client libraries.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.3 static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials in source (only `package.json` `private: true` and a comment that demo passwords are on request).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.41` twice (hero + demo band).
- Demo cross-check: landing copy, nav (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me`), patient-card sections, dashboard worklists, and footer version `0.17.41` taken from https://demo.arenibus.com/ assets dated 2026-09-04.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML shell is stable; the **JS bundle hash** (`/assets/index-*.js`) and the `im="X.Y.Z"` constant inside it are the version source of truth. Re-fetch `index.html` every run — do not reuse yesterday's filename.
- Clinician-facing patient **card sections** (Klinický obraz, Prevencia a hlásenia, Transplantácia, Posudky a potvrdenia, Dokumentácia a právne úkony) and the dashboard worklists (vyžiadaná dokumentácia, PN, SVLZ) decide card *copy*, even when they have no top-level nav item.
- `/me` (Môj profil) is user-facing but not a primary clinical module. Fold staff vaccinations and qualifications into Prevádzka; do not add an eleventh tile.
- Next.js 16.3.4 is still inside the 7-day window until 2026-09-07.

---

### Run 8 — 2026-09-04 (Demo alignment v0.17.42 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 0
- Low: 2 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo JS bundle `/assets/index-fQhrYvRO.js` embeds `im="0.17.42"`. Marketing badges still said **v0.17.41** from Run 7. The static HTML footer on the demo landing still reads `Arenibus v 0.17.41` until client hydration; the JS constant remains the source of truth per standing checks.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle constant changes.
- **Fixed:** Both badges now read `v0.17.42` via `DEMO_VERSION`.

##### 2. Demo username `<code>` used hardcoded white text
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** `demo-lekar` / `demo-sestra` codes used `text-white` on the brand teal band. In dark mode `--brand` is `#2dd4bf`, and white-on-teal fails WCAG 2.1 AA (same class of defect as the earlier `hover:text-white` finding).
- **Recommendation:** Use `text-brand-text` for any text on the demo brand band; never introduce raw `text-white` there.
- **Fixed:** Both `<code>` elements now use `text-brand-text`.

##### 3. Mobile menu `aria-controls` pointed at a missing node when closed
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** The menu was mounted only when `mobileMenuOpen` was true, so `aria-controls="mobile-menu"` referenced a non-existent `id` while the menu was closed.
- **Recommendation:** Keep `#mobile-menu` in the DOM and toggle visibility with `hidden` / CSS.
- **Fixed:** Menu container is always rendered; closed state uses the `hidden` attribute and `hidden` class.

##### 4. Next.js 16.3.4 remains inside the 7-day feature-vetting window
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.4` (2026-08-31). No newer 16.3.x security release. Four days old as of this run; re-evaluate on or after 2026-09-07. `npm audit` reports 0 vulnerabilities on `next@16.3.3`.

##### 5. Largest JS chunk baseline (Turbopack vs local webpack)
- **Severity:** Info
- **Category:** Performance
- **Description:** Local `npx next build --webpack` emits a ~241 KB chunk; CI Turbopack (`npm run build`) previously recorded ~229 KB for the homepage client bundle. No new client libraries. Treat Turbopack CI output as the production baseline.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npx next build --webpack`: static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; no `noindex`; exactly one `<h1>`; version `v0.17.42` twice; `#mobile-menu` present when closed.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-fQhrYvRO.js` constant `im="0.17.42"`; primary nav paths unchanged from Run 7 (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.41` (expected until this release lands).

#### Self-improvement notes
- Prefer the demo JS `im="X.Y.Z"` constant over the pre-hydration HTML footer when the two disagree after a redeploy.
- Ban any `text-white` on the demo brand band (not only `hover:text-white`); use `text-brand-text`.
- Keep `#mobile-menu` in the DOM whenever `aria-controls="mobile-menu"` is declared.
- Local webpack chunk sizes are not directly comparable to CI Turbopack baselines — record which bundler produced the figure.

---

### Run 9 — 2026-09-04 (Repeat pass after Run 8 fixes)

#### Summary
- Critical: 0
- High: 0
- Medium: 0
- Low: 0
- Info: 2 remaining (documented; not actionable)

#### Findings
No new fixable defects. Re-verified:

- Demo bundle still `index-fQhrYvRO.js` with `im="0.17.42"`; marketing `DEMO_VERSION` matches.
- No `text-white` remains in `app/page.tsx`.
- `#mobile-menu` stays in the DOM; Escape close and Slovak `aria-label`s intact.
- `npm audit` 0 vulnerabilities; Next.js 16.3.4 still inside the 7-day window until 2026-09-07.
- Sitemap `<lastmod>` already `2026-09-04` for all three URLs; no content-date drift to refresh.

#### Self-improvement notes
- After implementing fixes in a run, immediately re-fetch the demo bundle hash before closing the loop — patch releases can land between audit passes on the same calendar day.

---

### Run 10 — 2026-09-05 (Demo alignment v0.17.49 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-04 22:24 UTC (`index-TgeuO9yB.js`, HTML `Last-Modified: Fri, 04 Sep 2026 22:24:58 GMT`). The bundle constant is **Arenibus v0.17.49**. Marketing badges still said **v0.17.42** from Run 8/9.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle constant changes.
- **Fixed:** Both badges now read `v0.17.49` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.42 and v0.17.49
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work in the patient card, HD/PD guides, billing and operations: čakacia listina na transplantáciu, doklady o úhrade, vzory SOAP textov, vlastné typy návštev, KDIGO 2024, pripočítateľné položky, sprievodca zaradením do HD/PD, Nexadia karty, spotrebný materiál, zaškolenia na prístrojoch, vyšetrenie a zaškolenie PD, súhrn na odovzdanie služby, oznámenia o platenej starostlivosti, oznamy o zastupovaní a protokoly odovzdania dokumentácie.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, PD, Kalendár, Vykazovanie, and Prevádzka. Laboratórium, Moje zoznamy, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-04 after a content release.
- **Fixed:** All three now `2026-09-05`.

##### 4. Next.js 16.3.4 remains inside the 7-day feature-vetting window
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.4` (2026-08-31). No newer 16.3.x security release. Five days old as of 2026-09-05. `npm audit` reports 0 vulnerabilities on `next@16.3.3`.
- **Recommendation:** Re-evaluate on or after 2026-09-07. Do not skip the window for this non-security follow-up.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1mkbuudhndal5.js` is 228 922 bytes — unchanged from Run 8. No new client libraries.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.3 static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm ci` audit: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.49` twice (hero + demo band); `#mobile-menu` present when closed.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-TgeuO9yB.js` constant `im="0.17.49"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New chunks: `TemplatePicker`, `GuideSteps`, `MonitorsPage` (Nexadia Sync 7 cards).
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the `im="X.Y.Z"` constant inside the new file is the version source of truth.
- New clinician surfaces since v0.17.42 that still do **not** get their own tile: sprievodca zaradením (GuideSteps), vzory zápisov (TemplatePicker), Nexadia karty (admin Monitory), súhrn na odovzdanie služby (handoff), doklady o úhrade (patient card), oznamy a protokoly odovzdania (Prevádzka / Poskytovateľ).
- Next.js 16.3.4 remains inside the 7-day window until 2026-09-07.

---

### Run 11 — 2026-09-06 (Demo alignment v0.17.61 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-05 22:03 UTC (`index-Bx6pU547.js`, HTML `Last-Modified: Sat, 05 Sep 2026 22:03:43 GMT`). The bundle constant is **Arenibus v0.17.61**. Marketing badges still said **v0.17.49** from Run 10.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle constant changes.
- **Fixed:** Both badges now read `v0.17.61` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.49 and v0.17.61
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work in the patient card, billing, operations and reports: paliatívne poučenie (§ 6ba), individuálny očkovací plán, sprístupňovanie dokumentácie, žiadosti o nápravu, domáca starostlivosť a doplnkové ordinačné hodiny, oprava zápisu (§ 21), hemodiafiltrácia, PET a Kt/V, kniha eliminačných výkonov, potvrdenie neodkladnej starostlivosti, poistenci EÚ, oznámenia poisťovni (režim / alkohol / úrazy), sústavné vzdelávanie, register nežiaducich udalostí, zmluvy s poisťovňami a kniha prehliadok mŕtvych.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, PD, Moje zoznamy, Vykazovanie, and Prevádzka. Kalendár, Laboratórium, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-05 after a content release.
- **Fixed:** All three now `2026-09-06`.

##### 4. Next.js 16.3.4 remains inside the 7-day feature-vetting window
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.4` (2026-08-31). No newer 16.3.x security release. Six days old as of 2026-09-06. `npm audit` reports 0 vulnerabilities on `next@16.3.3`.
- **Recommendation:** Re-evaluate on or after 2026-09-07. Do not skip the window for this non-security follow-up.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1mkbuudhndal5.js` is 228 922 bytes — unchanged from Run 10. No new client libraries.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.3 static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm ci` audit: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.61` twice (hero + demo band); `#mobile-menu` present when closed.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Bx6pU547.js` constant `im="0.17.61"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New chunks since v0.17.49: `education-*`, `euInsured-*`, `VaccinationSection-*`.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the `im="X.Y.Z"` constant inside the new file is the version source of truth.
- New clinician surfaces since v0.17.49 that still do **not** get their own tile: paliatívne poučenie, žiadosti o nápravu, kniha prehliadok mŕtvych, register nežiaducich udalostí, poistenci EÚ, potvrdenie neodkladnej starostlivosti, sústavné vzdelávanie, zmluvy s poisťovňami, sprístupňovanie dokumentácie.
- Next.js 16.3.4 remains inside the 7-day window until 2026-09-07.

---

### Run 12 — 2026-09-07 (Demo alignment v0.17.70 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 2 (fixed)
- Info: 1 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-06 20:09 UTC (`index-D0eXchoX.js`, HTML `Last-Modified: Sun, 06 Sep 2026 20:09:20 GMT`). The bundle constant is **Arenibus v0.17.70**. Marketing badges still said **v0.17.61** from Run 11.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle constant changes.
- **Fixed:** Both badges now read `v0.17.70` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.61 and v0.17.70
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work in the patient card, billing, operations and reports: osobná starostlivosť (§ 12b), lekárske predpisy a poukazy, poučenie o živom darcovi, zákazy sprístupňovania, žiadosti Sociálnej poisťovne, eZápis, lekárske správy, odporúčania a konzultácie medzi pracovníkmi, opravná dávka O, služby súvisiace so starostlivosťou, poistenci mimo EÚ, písomná forma dokumentácie, dokumenty systému kvality a podklady pre kontrolu a dozor.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Moje zoznamy, Vykazovanie, and Prevádzka. HD, PD, Kalendár, Laboratórium, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-06 after a content release.
- **Fixed:** All three now `2026-09-07`.

##### 4. Next.js 16.3.4 passed the 7-day feature-vetting window
- **Severity:** Low
- **Category:** Security / Code Quality
- **File(s):** `package.json`, `package-lock.json`
- **Description:** `next@16.3.4` (2026-08-31) is a non-security follow-up (AVIF re-enable plus three bug fixes, including Turbopack `crossOrigin`). The window closed on 2026-09-07. Latest stable remains 16.3.4; no newer 16.3.x security release.
- **Recommendation:** Adopt 16.3.4 now that the window has elapsed; keep `images.unoptimized: true` so the AVIF change does not apply.
- **Fixed:** Upgraded `next` and `eslint-config-next` to exact `16.3.4`. `npm audit` reports 0 vulnerabilities.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 11 (filename hash changed with the Next.js bump). No new client libraries.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.70` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-D0eXchoX.js` constant `im="0.17.70"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on the existing patient-card / operations / billing / reports pages rather than new nav tiles.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the `im="X.Y.Z"` constant inside the new file is the version source of truth.
- New clinician surfaces since v0.17.61 that still do **not** get their own tile: osobná starostlivosť (§ 12b), lekárske predpisy a poukazy, poučenie o živom darcovi, zákazy sprístupňovania, žiadosti Sociálnej poisťovne, eZápis, lekárske správy, odporúčania, konzultácie medzi pracovníkmi, opravná dávka O, služby súvisiace so starostlivosťou, písomná forma dokumentácie, dokumenty systému kvality, podklady pre kontrolu a dozor, hlásenia prenosných ochorení, lekárske ožiarenia, preprava, biomedicínsky výskum.
- Next.js 16.3.4 is now the production baseline. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.

---

### Run 13 — 2026-09-08 (Demo alignment v0.17.74 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 1 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-07 18:47 UTC (`index-D0ODKnOH.js`, HTML `Last-Modified: Mon, 07 Sep 2026 18:47:26 GMT`). The bundle contains **Arenibus v0.17.74**. Marketing badges still said **v0.17.70** from Run 12.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** Both badges now read `v0.17.74` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.70 and v0.17.74
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work in the patient card, HD, billing, operations and reports: návrh transplantácie pred dialýzou, hlásenia prenosných ochorení, lekárske ožiarenia, preprava, biomedicínsky výskum, zbavenie mlčanlivosti, evidencia úmrtia, serologický status a prevádzkové určenie prístroja vrátane vyhradenia pre akútne stavy, hosťovské ošetrenia, indikátory kvality, objednania a lehota poskytnutia, podklady ku kontrole očkovania, poistenci bez verejného poistenia (I/J/K), lieky a pomôcky podľa pôvodu (§ 79a), karta zariadenia, označenie zariadenia a informačný list, poistenie zodpovednosti a oznámenia zmien údajov v povolení.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Hemodialýza, Moje zoznamy, Vykazovanie, and Prevádzka. Evidencia návštev, PD, Kalendár, Laboratórium, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-07 after a content release.
- **Fixed:** All three now `2026-09-08`.

##### 4. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 12. No new client libraries. Latest stable Next.js remains `16.3.4`; no newer 16.3.x / 16.4 stable and no critical security patch.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.74` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-D0ODKnOH.js` contains `0.17.74`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on existing patient-card / HD / operations / billing / reports pages rather than new nav tiles.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the `im="X.Y.Z"` form was minified away — `0.17.74` still appears uniquely in the bundle).
- New clinician surfaces since v0.17.70 that still do **not** get their own tile: návrh transplantácie pred dialýzou, hlásenia prenosných ochorení, lekárske ožiarenia, preprava, biomedicínsky výskum, zbavenie mlčanlivosti, evidencia úmrtia, serológia a vyhradenie prístroja, hosťovské ošetrenia, indikátory kvality, objednania a lehota poskytnutia, poistenci I/J/K, pôvod liekov/pomôcok § 79a, karta zariadenia, označenie zariadenia, poistenie zodpovednosti, zmeny v povolení.
- Latest stable Next.js remains 16.3.4. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.

---

### Run 14 — 2026-09-09 (Demo alignment v0.17.76 & repeated audit)

#### Summary
- Critical: 0
- High: 2 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 1 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-08 22:02 UTC (`index-DPsOCBfy.js`, HTML `Last-Modified: Tue, 08 Sep 2026 22:02:48 GMT`). The bundle constant is **Arenibus v0.17.76** (`ip="0.17.76"`). Marketing badges still said **v0.17.74** from Run 13.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** Both badges now read `v0.17.76` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.74 and v0.17.76
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work: evidencia doručenia lekárskych správ, kanály objednania (osobne, telefonicky, NCZI, objednávací systém poskytovateľa), pôvod laboratórnych výsledkov (listinný list, elektronický záznam SVLZ, rozhranie laboratória), podklady pre revíznu kontrolu poisťovne / dozor orgánu / klinický audit (§ 9c), dispenzárne dávky a zoznam pracovníkov s oznámeniami a exportom do 65 rokov.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Evidencia Návštev, Kalendár, Laboratórium, Moje zoznamy, Vykazovanie, and Prevádzka. Správa Pacientov, HD, PD, and Audit copy remain accurate.

##### 3. `js-yaml` had a fixable high-severity advisory
- **Severity:** High (fixed, non-breaking; devDependency only)
- **Category:** Security
- **File(s):** `package-lock.json`
- **Description:** `npm audit` reported `js-yaml` 4.0.0–4.3.1 (transitive via `eslint` → `@eslint/eslintrc`) does not limit CPU use for empty merge sources (GHSA-2883-xcg3-v3hh). `npm audit --omit=dev` was already clean.
- **Recommendation:** `npm audit fix` without `--force`.
- **Fixed:** `js-yaml` is now 4.3.2. `npm audit` reports 0 vulnerabilities.

##### 4. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-08 after a content release.
- **Fixed:** All three now `2026-09-09`.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 13. No new client libraries. Latest stable Next.js remains `16.3.4`; no newer 16.3.x / 16.4 stable and no critical security patch.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities after the `js-yaml` bump.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.76` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-DPsOCBfy.js` constant `ip="0.17.76"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on existing encounter / calendar / lab / reports / billing / operations pages rather than new nav tiles.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `ip="0.17.76"`; `im="X.Y.Z"` remains minified away).
- New clinician surfaces since v0.17.74 that still do **not** get their own tile: evidencia doručenia lekárskych správ, kanály objednania vrátane NCZI, pôvod laboratórnych výsledkov, účely podkladov pre kontrolu (revízia / dozor / klinický audit § 9c), dispenzárne dávky, zoznam pracovníkov a export do 65 rokov.
- Latest stable Next.js remains 16.3.4. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.
- `npm audit fix` without `--force` remains safe each round; this run only moved `js-yaml` 4.3.1 → 4.3.2 (eslint toolchain).

---

### Run 15 — 2026-09-10 (Demo alignment v0.17.78 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 1 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-09 20:58 UTC (`index-98JRkcee.js`, HTML `Last-Modified: Wed, 09 Sep 2026 20:58:01 GMT`). The bundle constant is **Arenibus v0.17.78** (`ip="0.17.78"`). Marketing badges still said **v0.17.76** from Run 14.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** Both badges now read `v0.17.78` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.76 and v0.17.78
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work on the patient card, HD/PD records, billing and operations: poučenie o rozsahu starostlivosti a zmluve s poisťovňou, účasť na výučbe, prekážky / nežiaduce účinky očkovania a rozhodnutia RÚVZ, výpis dokumentácie, krízová situácia (§ 49k), dialyzačná vizita a zrušenie HD/PD záznamu s nahradením, mesačné kontroly adekvátnosti / anémie / fosfo-kalcia, bilancia tekutín a liečba peritonitídy, revízne nálezy dávky, evidencia záložných kópií, klinické audity a rozpis ordinačných hodín s predložením kraju.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, PD, Vykazovanie, and Prevádzka. Kalendár, Laboratórium, Moje zoznamy, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-09 after a content release.
- **Fixed:** All three now `2026-09-10`.

##### 4. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 14. No new client libraries. Latest stable Next.js remains `16.3.4` (2026-08-31); 16.4 is still canary-only and there is no newer 16.3.x security patch.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.78` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-98JRkcee.js` constant `ip="0.17.78"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on existing patient-card / HD / PD / billing / operations pages rather than new nav tiles.
- Second audit loop (same checklist): no further fixable defects.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `ip="0.17.78"`; `im="X.Y.Z"` remains minified away).
- New clinician surfaces since v0.17.76 that still do **not** get their own tile: poučenie o rozsahu starostlivosti a zmluve s poisťovňou, účasť na výučbe, RÚVZ / prekážky / nežiaduce účinky očkovania, výpis dokumentácie, krízová situácia § 49k, dialyzačná vizita, zrušenie HD/PD záznamu s nahradením, peritonitída, evidencia záložných kópií, klinické audity, ordinačné hodiny s predložením kraju, revízne nálezy dávky.
- Latest stable Next.js remains 16.3.4. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.

---

### Run 16 — 2026-09-11 (Demo alignment v0.17.96 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 1 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-10 17:38 UTC (`index-Xz5Re8hi.js`, HTML `Last-Modified: Thu, 10 Sep 2026 17:38:24 GMT`). The bundle constant is **Arenibus v0.17.96** (`ip="0.17.96"`). Marketing badges still said **v0.17.78** from Run 15.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** Both badges now read `v0.17.96` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.78 and v0.17.96
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work on the patient card, HD records, lab, dashboard worklists, operations/provider and audit: odstúpenie od dohody o ZS, oznámenia podozrenia (§ 79 ods. 4), osobitné komentáre k HD záznamu aj po overení, identifikátory žiadanky a vzorky (§ 19a) s prehľadom nevybavených žiadaniek, registrácia v komore a identifikátor NRZP, vykazovacie jednotky, informácie na webové sídlo a filter pokusov o neoprávnený prístup (§ 22 ods. 6).
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Hemodialýza, Laboratórium, Moje zoznamy, Prevádzka, and Audit. Evidencia Návštev, PD, Kalendár, and Vykazovanie copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-10 after a content release.
- **Fixed:** All three now `2026-09-11`.

##### 4. CI link check failed on `dataprotection.gov.sk` at the network layer
- **Severity:** Medium
- **Category:** Deployment
- **File(s):** `scripts/check-links.mjs`
- **Description:** First push of this run (`34549407421`) failed at **Check web links**: `https://dataprotection.gov.sk/` → `fetch failed` after the existing 3 retries. The URL still returns HTTP 200 from this environment and from a browser; GitHub Actions IPs are occasionally dropped by the official host. A hard fail blocked deploy even though the privacy-page href is correct.
- **Recommendation:** Keep verifying the official URL, but treat network-layer failures (timeout / fetch failed) on this known official host as a warning after retries. Do not ignore HTTP 404/5xx.
- **Fixed:** Browser-compatible User-Agent, 4 retries / 20 s timeout, and a `FLAKY_OFFICIAL_HOSTS` warning path for `dataprotection.gov.sk` network errors only.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 15. No new client libraries. Latest stable Next.js remains `16.3.4`; 16.4 is still canary-only and there is no newer 16.3.x security patch.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally. First CI run `34549407421` failed on `dataprotection.gov.sk` (`fetch failed`); checker hardened and re-pushed.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.96` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Xz5Re8hi.js` constant `ip="0.17.96"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on existing patient-card / HD / lab / operations / provider / audit pages rather than new nav tiles.
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.78` (expected until this release lands).
- Second audit loop (same checklist): no further fixable defects after the link-check hardening.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `ip="0.17.96"`; `im="X.Y.Z"` remains minified away).
- New clinician surfaces since v0.17.78 that still do **not** get their own tile: odstúpenie od dohody o ZS, oznámenia podozrenia (§ 79 ods. 4), osobitné komentáre k HD záznamu, identifikátory žiadanky/vzorky § 19a, nevybavené žiadanky na SVLZ, registrácia v komore a NRZP, vykazovacie jednotky, informácie na webové sídlo, filter pokusov o neoprávnený prístup § 22 ods. 6.
- Latest stable Next.js remains 16.3.4. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.

---

### Run 17 — 2026-09-12 (Demo alignment v0.17.107 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-11 21:25 UTC (`index-Cy10Me7i.js`, HTML `Last-Modified: Fri, 11 Sep 2026 21:25:27 GMT`). The bundle constant is **Arenibus v0.17.107** (`fp="0.17.107"`). Marketing badges still said **v0.17.96** from Run 16.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** Both badges now read `v0.17.107` via `DEMO_VERSION`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.96 and v0.17.107
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work on the patient card, HD records, encounter procedures, billing reports, operations and worker identity: DPN liečebný režim, vychádzky a preukaz o trvaní, písomný nesúhlas pri osobnej starostlivosti (§ 12b), informovanie zmluvného lekára o očkovaní, súpis dokumentácie, hlásenie pre NTO k čakacej listine, kontrola revíznych pravidiel pri zápise výkonov, oprava HD záznamu so zachovaním predchádzajúcich znení, evidencia napojenia na mimotelový okruh, oznámenia o kódoch náhrady, údaje pre ministerstvo zdravotníctva (§ 79 ods. 1 písm. zh), mesačný výkaz pre Sociálnu poisťovňu (§ 233 ods. 2 písm. c), preukaz ePZP s oznámením národnému centru (§ 80 ods. 1 písm. h) a preukázateľné oboznámenie pracovníkov s dokumentmi systému kvality.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, Moje zoznamy, Vykazovanie, and Prevádzka. PD, Kalendár, Laboratórium, and Audit copy remain accurate.

##### 3. Sitemap `<lastmod>` was a day behind
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-11 after a content release.
- **Fixed:** All three now `2026-09-12`.

##### 4. Next.js 16.3.5 published, not adopted
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** `next@16.3.5` was released 2026-09-11 19:15 UTC (npm `modified` 23:52 UTC). GitHub notes backports only: `next/image` disk LRU empty-file skips, standalone NFT for adapters, CSP nonce on loading/template scripts, and a `use cache` prerender-signal fix. No GHSA / critical security advisory. This site is a Linux static export with `images.unoptimized: true`, so the image-cache and standalone changes do not apply. Feature-release 7-day window is not required for a patch, but a same-day unvetted bump is still deferred.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 16. No new client libraries.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.107` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Cy10Me7i.js` constant `fp="0.17.107"`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin). New clinician surfaces live on existing patient-card / HD / encounter / billing / operations / worker-profile pages rather than new nav tiles.
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.96` (expected until this release lands).
- Second audit loop (same checklist): no further fixable defects after the content alignment.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `fp="0.17.107"`; `im="X.Y.Z"` and `ip="X.Y.Z"` remain minified away). Search for a unique `X.Y.Z` near the copyright year constants, not only `ip=` / `im=`.
- New clinician surfaces since v0.17.96 that still do **not** get their own tile: DPN liečebný režim / vychádzky / preukaz o trvaní, písomný nesúhlas § 12b, informovanie zmluvného lekára o očkovaní, súpis dokumentácie, hlásenie NTO, revízne pravidlá pri výkonoch, oprava HD záznamu, napojenie na mimotelový okruh, kódy náhrady, údaje pre MZ SR § 79 ods. 1 písm. zh, výkaz pre Sociálnu poisťovňu § 233 ods. 2 písm. c, ePZP § 80 ods. 1 písm. h, oboznámenie s dokumentmi kvality.
- `next@16.3.5` (2026-09-11) is a same-day patch without a critical advisory and does not change this static export. Re-evaluate after 2026-09-18 or on a GHSA.

### Run 18 — 2026-09-13 (Demo still v0.17.107, contact-form autocomplete & repeated audit)

#### Summary
- Critical: 0
- High: 0
- Medium: 1 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Live demo is unchanged since Run 17; marketing copy already matches
- **Severity:** Info (verified, no content drift)
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ is still the 2026-09-11 21:25 UTC deploy (`index-Cy10Me7i.js`, HTML `Last-Modified: Fri, 11 Sep 2026 21:25:27 GMT`). The bundle constant remains **Arenibus v0.17.107** (`fp="0.17.107"`). Primary clinician nav is unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**. Lazy chunks and Last-Modified stamps match Run 17. Feature-card copy already covers the clinician surfaces shipped through v0.17.107. Production https://arenibus.polascin.net/ already advertised `v0.17.107` before this run.
- **Recommendation:** Keep `DEMO_VERSION` and the ten-card layout. Do not invent tiles for admin-only screens or `/me`.
- **Fixed:** No feature-card or version change required.

##### 2. Contact form inputs lacked programmatic purpose (WCAG 1.3.5)
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** Name and e-mail fields had Slovak `<label>` elements and `type="email"` on the address field, but neither declared `autoComplete`. WCAG 2.1 AA 1.3.5 requires an autocomplete token for fields that collect the user's own name and e-mail so password managers and assistive tech can identify purpose.
- **Recommendation:** Set `autoComplete="name"` and `autoComplete="email"` on the contact inputs.
- **Fixed:** Both tokens added.

##### 3. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-12 after a new release day.
- **Fixed:** All three now `2026-09-13`.

##### 4. Next.js 16.3.5 remains deferred
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.5` (published 2026-09-11; npm `modified` 2026-09-12 23:53 UTC). No GHSA / critical advisory found. This site is a Linux static export with `images.unoptimized: true`, so the image-cache, standalone NFT, CSP-nonce, and `use cache` backports do not apply. The 7-day feature window runs until 2026-09-18.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 17. Autocomplete tokens do not add a client library.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>`; ten feature modules; version `v0.17.107` twice (hero + demo band); `#mobile-menu` present when closed; no `text-white`; contact inputs emit `autocomplete="name"` and `autocomplete="email"`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Cy10Me7i.js` constant `fp="0.17.107"`; all lazy chunks share Last-Modified `Fri, 11 Sep 2026 21:25:27 GMT`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with `v0.17.107` (browser User-Agent). Short/bot User-Agents can receive WAF 466 on `/robots.txt`, `/sitemap.xml`, `/privacy/`, `/terms/` — homepage still 200. Use a full browser UA for smoke checks.
- Second audit loop (same checklist): no further fixable defects after the autocomplete and sitemap updates.

#### Self-improvement notes
- When the demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, treat feature-card copy as already aligned and spend the run on WCAG / SEO / dependency checks instead of inventing module tiles.
- Contact name and e-mail fields must keep HTML `autocomplete` tokens (`name`, `email`) in addition to `<label>` — 1.3.5 is easy to miss when labels already exist.
- Production WAF (OpenResty) may return HTTP 466 for `/robots.txt`, `/sitemap.xml`, `/privacy/`, and `/terms/` when the client sends a short or bot-like User-Agent. Homepage often still returns 200. Smoke-check those paths with a full Chrome/Firefox UA, not `Mozilla/5.0` alone.
- `next@16.3.5` is still inside the feature-vetting window (re-evaluate 2026-09-18 or on a GHSA).

---

### Run 19 — 2026-09-14 (Demo still v0.17.107, skip links, theme-toggle state & JSON-LD version)

#### Summary
- Critical: 0
- High: 0
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Live demo is unchanged since Run 17; marketing copy already matches
- **Severity:** Info (verified, no content drift)
- **Category:** Content
- **File(s):** `app/page.tsx`, `lib/site.ts`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ is still the 2026-09-11 21:25 UTC deploy (`index-Cy10Me7i.js`, HTML `Last-Modified: Fri, 11 Sep 2026 21:25:27 GMT`). The bundle constant remains **Arenibus v0.17.107** (`fp="0.17.107"`). Primary clinician nav is unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**. Lazy chunks and Last-Modified stamps match Run 17/18. A `/for-worker` string in the bundle is an API suffix on quality-document acknowledgements, not a nav tile. Feature-card copy already covers the clinician surfaces shipped through v0.17.107. Production https://arenibus.polascin.net/ already advertised `v0.17.107` before this run.
- **Recommendation:** Keep `DEMO_VERSION` and the ten-card layout. Do not invent tiles for admin-only screens or `/me`.
- **Fixed:** Moved the version constant to `lib/site.ts` so the homepage badges and JSON-LD cannot drift. No feature-card change required.

##### 2. Legal pages had no skip-to-content target (WCAG 2.4.1)
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/privacy/page.tsx`, `app/terms/page.tsx`
- **Description:** `/privacy/` and `/terms/` have a sticky nav but no bypass link. The homepage already offered „Preskočiť na hlavný obsah“ → `#main-content`. Keyboard users landing on the legal pages had to tab through the logo and back-link before reaching the article.
- **Recommendation:** Add the same skip link and `id="main-content"` on `<main>` of every route.
- **Fixed:** Both legal pages now match the homepage skip pattern. Keep the markup inline — `npm run check:links` resolves fragments against the file that declares the `href`, so a shared skip component fails the check.

##### 3. Dark-mode toggle did not expose pressed state (WCAG 4.1.2)
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** The theme button had a Slovak `aria-label` but no `aria-pressed`. Assistive tech could not tell whether dark mode was currently on.
- **Recommendation:** Set `aria-pressed={darkMode}` on the toggle.
- **Fixed:** The control now emits `aria-pressed="false"` (default / light) and updates with the stored preference.

##### 4. JSON-LD omitted the live demo version and logo
- **Severity:** Low
- **Category:** SEO
- **File(s):** `app/layout.tsx`, `lib/site.ts`
- **Description:** `SoftwareApplication` structured data had name, description, URL, category and offers, but not `softwareVersion`, `inLanguage`, or `logo`. The version lived only in the client homepage badges, so a crawler could not see that the advertised release is 0.17.107.
- **Recommendation:** Drive JSON-LD `softwareVersion` from the same constant as the badges.
- **Fixed:** Shared `DEMO_VERSION` / `DEMO_VERSION_PLAIN` in `lib/site.ts`; JSON-LD now includes `softwareVersion: "0.17.107"`, `inLanguage: "sk"`, and `logo` pointing at the 1200×630 OG image.

##### 5. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-13 after a new release day.
- **Fixed:** All three now `2026-09-14`.

##### 6. Next.js 16.3.5 remains deferred
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.5` (published 2026-09-11; npm `modified` 2026-09-12 23:53 UTC). No newer 16.3.x and no GHSA / critical advisory. This site is a Linux static export with `images.unoptimized: true`, so the image-cache, standalone NFT, CSP-nonce, and `use cache` backports do not apply. The 7-day feature window runs until 2026-09-18.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 7. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 18. Skip links and JSON-LD fields do not add a client library.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 26 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments (including new `#main-content` on privacy/terms), PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.107` twice on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.107`; `#main-content` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Cy10Me7i.js` constant `fp="0.17.107"`; lazy chunks share Last-Modified `Fri, 11 Sep 2026 21:25:27 GMT`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with `v0.17.107` (browser User-Agent).
- Second audit loop (same checklist): no further fixable defects after the skip-link, `aria-pressed`, JSON-LD, and sitemap updates.

#### Self-improvement notes
- When the demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, treat feature-card copy as already aligned and spend the run on WCAG / SEO / dependency checks.
- Keep skip-to-content markup **inline** on each page. `scripts/check-links.mjs` resolves `#fragments` against the file that contains the `href`; extracting a shared `SkipToContent` component fails the check even when every consumer has the matching `id`.
- JSON-LD `softwareVersion` must come from the same `lib/site.ts` constant as the homepage `DEMO_VERSION` badges.
- Theme toggle must keep `aria-pressed` in addition to the Slovak `aria-label`.
- A `/for-worker` hit in the demo bundle is the quality-document acknowledgements API suffix, not a clinician nav module.

### Run 20 — 2026-09-15 (Demo still v0.17.107, demo-band contrast & theme toggle on legal pages)

#### Summary
- Critical: 0
- High: 0
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Live demo is unchanged since Run 17; marketing copy already matches
- **Severity:** Info (verified, no content drift)
- **Category:** Content
- **File(s):** `app/page.tsx`, `lib/site.ts`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ is still the 2026-09-11 21:25 UTC deploy (`index-Cy10Me7i.js`, HTML `Last-Modified: Fri, 11 Sep 2026 21:25:27 GMT`). The bundle constant remains **Arenibus v0.17.107** (`fp="0.17.107"`). Primary clinician nav is unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**. Lazy chunks and Last-Modified stamps match Run 17–19. Feature-card copy already covers the clinician surfaces shipped through v0.17.107. Production https://arenibus.polascin.net/ already advertised `v0.17.107` before this run.
- **Recommendation:** Keep `DEMO_VERSION` and the ten-card layout. Do not invent tiles for admin-only screens or `/me`.
- **Fixed:** No feature-card or version change required.

##### 2. Demo-access card and OIDC caption failed WCAG 2.1 AA in light mode
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`, `scripts/check-contrast.mjs`
- **Description:** Small text on the teal demo band used translucent overlays (`bg-surface/10`, inner `bg-surface/20`) plus `text-brand-text/90` and nested `opacity-75`. Effective contrast in light mode dropped to ~2.6:1 for labels and ~3.3:1 for 90 % white on the frosted card — below the 4.5:1 AA floor for `text-xs` / `text-sm`. The OIDC caption used `text-brand-text/80` (4.13:1 on `--brand`). The token checker only sampled solid `brand-text` on `--brand`, so the failure was invisible to CI.
- **Recommendation:** Put small text on opaque `surface` / `surface-2` cards. Do not combine Tailwind opacity modifiers with `text-brand-text` on the demo gradient.
- **Fixed:** Demo-access block is a solid `bg-surface` card with `foreground` / `foreground-2` copy and a brand primary CTA. OIDC caption is full-opacity `text-brand-text`. Contrast checker now includes version/role badge, brand-on-surface, username code, theme-toggle icon, and OIDC-on-brand-strong pairs.

##### 3. Legal pages had no theme toggle
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/theme-toggle.tsx`, `app/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`
- **Description:** Root layout applies `data-theme` from `localStorage` on every route, so `/privacy/` and `/terms/` rendered in dark mode after a homepage visit, but those sticky navs had no control to switch back. Keyboard and screen-reader users could not change the pressed state of the theme except on `/`.
- **Recommendation:** Keep one shared client `ThemeToggle` (`aria-label` + `aria-pressed`) on every sticky nav. Leave skip links inline.
- **Fixed:** Extracted `app/theme-toggle.tsx` and mounted it on `/`, `/privacy/`, and `/terms/`.

##### 4. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-14 after a new release day.
- **Fixed:** All three now `2026-09-15`.

##### 5. Next.js 16.3.5 remains deferred
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.5` (published 2026-09-11; npm `modified` 2026-09-14 23:54 UTC). No GHSA / critical advisory for 16.3.5 (August 2026 RCEs were patched in 16.3.3, already superseded by 16.3.4 on this site). This site is a Linux static export with `images.unoptimized: true`, so the image-cache, standalone NFT, CSP-nonce, and `use cache` backports do not apply. The 7-day feature window runs until 2026-09-18.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 6. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 19. Theme toggle extraction does not add a library; legal pages now hydrate a small client island.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 40 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally.
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.107` twice on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.107`; `#main-content` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed` on all three routes; no `text-brand-text/80` or nested `opacity-75` on the demo band.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-Cy10Me7i.js` constant `fp="0.17.107"`; lazy chunks share Last-Modified `Fri, 11 Sep 2026 21:25:27 GMT`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with `v0.17.107` (browser User-Agent).
- Second audit loop (same checklist): no further fixable defects after the contrast and theme-toggle updates.

#### Self-improvement notes
- When the demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, treat feature-card copy as already aligned and spend the run on WCAG / SEO / dependency checks.
- Never put `text-xs` / `text-sm` `text-brand-text` on a translucent `bg-surface/10` overlay of the teal demo gradient — compute the composited colour; the solid-token checker will not catch it. Prefer an opaque `surface` card.
- Do not use nested Tailwind opacity (`text-brand-text/90` plus `opacity-75`) on the demo band.
- Theme toggle must appear on every sticky nav (`/`, `/privacy/`, `/terms/`), not only the homepage, because `themeInit` in the root layout applies `data-theme` globally.
- `next@16.3.5` is still inside the feature-vetting window (re-evaluate 2026-09-18 or on a GHSA).

### Run 21 — 2026-09-16 (Demo alignment v0.17.127, landmarks & visible form labels)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-15 17:52 UTC (`index-D0XHUNqt.js`, HTML `Last-Modified: Tue, 15 Sep 2026 17:52:19 GMT`). The bundle constant is **Arenibus v0.17.127** (`yp="0.17.127"`). Marketing badges and JSON-LD still said **v0.17.107** from Run 17–20.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** `DEMO_VERSION` is now `v0.17.127`; JSON-LD `softwareVersion` follows `DEMO_VERSION_PLAIN`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.107 and v0.17.127
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work on the patient card, encounters, HD records, lab panels, reports, billing, operations and worker identity: poučenie § 6ba, odmietnutie poučenia § 6 ods. 3, súhlas keď sa nevyžaduje § 6 ods. 9, očkovací plán so zachovaním predchádzajúceho znenia, DPN starostlivosť poskytnutá zamestnancovi po odpracovaní zmeny § 12a ods. 4, register nahliadnutí § 25 (výpisky, kópie, pseudonymizácia, osvedčený podpis § 18 ods. 4), výpis § 24, KDIGO eGFRcr-cys (cystatín C), telekonzultácia a videokonzultácia, zápis osobou určenou podľa § 18 ods. 1, HD spôsob napojenia / kanylácia AVF / metóda MEL / typ substitúcie, sérológia § 19 ods. 2 písm. g) na pridelenie prístroja, dohoda o nevyhotovení listinnej podoby výsledku § 8 ods. 9, podklady ku kontrole očkovania RÚVZ § 14 ods. 1 vyhl. 585/2008, pre-flight kontrola dávok 751a, odchýlky z vlastných zásob, odpoveď poisťovne na oznámenia, interný systém hodnotenia bezpečnosti pacienta § 79 ods. 1 písm. ba/bb a § 9b, export pracovníkov do 65 rokov § 79 ods. 1 písm. bh, náhradný odborný zástupca.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, Laboratórium, Moje zoznamy, Vykazovanie, and Prevádzka. PD, Kalendár, and Audit copy remain accurate.

##### 3. Homepage lacked a `<main>` landmark; sticky navs were not in `<header>`
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`
- **Description:** Legal pages already wrapped article content in `<main id="main-content">`. The homepage skip link targeted a hero `<section id="main-content">`, so there was no `main` landmark. Sticky navs on all three routes were bare `<nav>` elements without a `header`. Keyboard and landmark navigation therefore treated the marketing homepage differently from `/privacy/` and `/terms/`.
- **Recommendation:** Put skip-link targets on `<main>` on every route. Wrap each sticky nav (and the homepage mobile menu) in `<header>`.
- **Fixed:** Homepage content from hero through contact is now `<main id="main-content">`. All three routes wrap the sticky nav in `<header className="sticky top-0 z-50">` with `aria-label="Hlavná navigácia"` on `<nav>`. Skip-link markup stays inline.

##### 4. Contact form labels were screen-reader-only
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`
- **Description:** Name, e-mail and message fields had `sr-only` labels plus placeholders. Autocomplete tokens from Run 18 remained. Sighted users lost the label once they started typing, and the e-mail placeholder said „Váš email“ (without hyphen) rather than a sample address.
- **Recommendation:** Keep visible `<label>` text in addition to `autoComplete="name"` / `autoComplete="email"`.
- **Fixed:** Visible Slovak labels; e-mail placeholder is now `vas@email.sk`. Autocomplete tokens unchanged.

##### 5. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-15 after a new release day.
- **Fixed:** All three now `2026-09-16`.

##### 6. Next.js 16.3.5 remains deferred
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.5` (published 2026-09-11). No GHSA / critical advisory for 16.3.5 (August 2026 RCEs were patched in 16.3.3, already superseded by 16.3.4 on this site). 16.4 remains canary-only. This site is a Linux static export with `images.unoptimized: true`, so the image-cache, standalone NFT, CSP-nonce, and `use cache` backports do not apply. The 7-day feature window runs until 2026-09-18.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 7. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 20. Landmark wrappers and visible labels do not add a client library.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 42 token pairs pass WCAG 2.1 AA in both themes (added form-input `foreground` on `surface`); dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` network-layer WARN after retries; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.127` twice on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.127`; `<header>` + `<main id="main-content">` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed` on all three routes; visible contact labels.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-D0XHUNqt.js` constant `yp="0.17.127"`; lazy chunks share Last-Modified `Tue, 15 Sep 2026 17:52:19 GMT`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.107` (expected until this release lands).
- Second audit loop (same checklist): no further fixable defects after the version, copy, landmark, and label updates.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `yp="0.17.127"`). Search for a unique `X.Y.Z` near the copyright year constants, not only `ip=` / `im=` / `fp=`.
- New clinician surfaces since v0.17.107 that still do **not** get their own tile: poučenie § 6ba / odmietnutie poučenia / súhlas § 6 ods. 9, register nahliadnutí § 25, výpis § 24, DPN § 12a ods. 4, eGFRcr-cys, telekonzultácia, zápis určenou osobou § 18 ods. 1, HD MEL / AVF kanylácia, sérológia § 19 ods. 2 písm. g), listinná podoba § 8 ods. 9, RÚVZ očkovanie § 14 ods. 1 vyhl. 585/2008, pre-flight 751a, interný systém bezpečnosti § 9b, export do 65 rokov § 79 ods. 1 písm. bh.
- The homepage skip target must be `<main id="main-content">`, not a hero `<section>`. Legal pages already used `<main>`; keep the three routes consistent.
- Contact fields need a **visible** `<label>` in addition to `autoComplete` tokens and placeholders.
- `next@16.3.5` is still inside the feature-vetting window (re-evaluate 2026-09-18 or on a GHSA).

### Run 22 — 2026-09-17 (Demo alignment v0.17.143, remaining demo-band pill contrast)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 2 remaining (not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-17 01:10 UTC (`index-C2fpkcGW.js`, HTML `Last-Modified: Thu, 17 Sep 2026 01:10:00 GMT`). The bundle constant is **Arenibus v0.17.143** (`yp="0.17.143"`). Marketing badges and JSON-LD still said **v0.17.127** from Run 21.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** `DEMO_VERSION` is now `v0.17.143`; JSON-LD `softwareVersion` follows `DEMO_VERSION_PLAIN`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.127 and v0.17.143
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Demo nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit; admin Cenník / Monitory / Poskytovateľ / Používatelia / Uchovávanie; user **Môj profil**). New clinician work on the patient card, encounters, HD enrolment, calendar, reports, billing and operations: spôsob poučenia § 6 ods. 2, vyjadrenie nespôsobilej osoby § 6 ods. 10, negatívny reverz, súhlasy na HD/PD/CVK/AVF/transfúziu/biopsiu, účasť na výučbe § 11 ods. 9, odstúpenie poskytovateľa § 12 ods. 16 a zánik dohody § 12 ods. 18, DPN ukončenie lekárom § 12a ods. 5 / preukaz o trvaní § 12a ods. 8 / potvrdenie pri výpadku IS § 12a ods. 17, osobná starostlivosť hospitalizácia a ukončenie § 12b ods. 8 / papierové potvrdenie § 12b ods. 11, listinný predpis ŠÚKL do eReceptu § 79 ods. 1 písm. r, návrh transplantácie § 79 ods. 1 písm. ad, doklad o hlásení RÚVZ § 4 vyhl. 585/2008 a zdroj nákazy § 11 ods. 14, ožiarenia § 44a / § 44d, oslobodenie od spoluúčasti za 1 km § 3 ods. 19 zák. 577/2004, 7-dňová platnosť potvrdenia VLD § 25 ods. 8, dispenzárna skupina vyhl. 127/2014, lehota plánovanej starostlivosti § 21 ods. 3 písm. h, správa pre VLD § 8 ods. 6, odporúčanie na špecializovanú starostlivosť a SVLZ § 8 ods. 4 a 8, overenie totožnosti pri telekonzultácii § 8 ods. 13, preddialyzačný skríning HBsAg/anti-HCV/anti-HIV, deň objednania a lehota 20 dní § 2a ods. 2, pôvod objednania § 79 ods. 1 písm. am) až ao), oznámenie o doplnkových hodinách a domácej starostlivosti § 79 ods. 1 písm. ap, poučenie o úhrade podľa cenníka § 19 ods. 7, doklad totožnosti poistenca bez verejného poistenia § 79b, klinický audit § 9d / § 79 ods. 1 písm. bc, informácie na webové sídlo § 79 ods. 1 písm. zj, priame odovzdanie dokumentácie § 23 ods. 9 a úschova krajom § 23 ods. 4 a 5.
- **Recommendation:** Keep the ten-card layout; fold new clinician work into the matching card. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, Kalendár, Moje zoznamy, Vykazovanie, and Prevádzka. PD, Laboratórium, and Audit copy remain accurate.

##### 3. Demo-band version pill still used a translucent overlay
- **Severity:** Medium
- **Category:** Accessibility
- **File(s):** `app/page.tsx`, `scripts/check-contrast.mjs`
- **Description:** Run 20 made the demo-access card opaque, but the `text-xs` version pill on the teal band still used `bg-surface/20 text-brand-text backdrop-blur-xs`. Composited white-on-frosted-teal falls below the 4.5:1 AA floor for small text; the solid-token checker did not see it.
- **Recommendation:** Put the pill on an opaque `surface` with `text-brand-strong`. Add the pair to `check:contrast`.
- **Fixed:** Pill is now `bg-surface text-brand-strong`; contrast checker includes `demo version pill (text-xs)`.

##### 4. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-16 after a new release day.
- **Fixed:** All three now `2026-09-17`.

##### 5. Next.js 16.3.5 remains deferred
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable is still `next@16.3.5` (published 2026-09-11; npm `modified` 2026-09-16 23:56 UTC). No newer 16.3.x and no GHSA / critical advisory (`npm audit` 0 vulnerabilities). This site is a Linux static export with `images.unoptimized: true`, so the image-cache, standalone NFT, CSP-nonce, and `use cache` backports do not apply. The 7-day feature window runs until 2026-09-18.
- **Recommendation:** Re-evaluate on 2026-09-18 or immediately if a GHSA appears. Do not use `npm audit fix --force`.

##### 6. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/1j_9b-l0n6u-t.js` is 228 922 bytes — same homepage client-bundle baseline as Run 21. Copy and pill class changes do not add a client library.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 44 token pairs pass WCAG 2.1 AA in both themes (added demo version pill `brand-strong` on `surface`); dark-palette parity holds.
- `npm run build`: Next.js 16.3.4 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.143` twice on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.143`; `<header>` + `<main id="main-content">` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed` on all three routes; visible contact labels; demo version pill is opaque `surface`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-C2fpkcGW.js` constant `yp="0.17.143"`; lazy chunks share Last-Modified `Thu, 17 Sep 2026 01:10:00 GMT`; primary nav paths unchanged (`/patients`, `/calendar`, `/dialysis`, `/peritoneal`, `/shortlists`, `/reports`, `/billing`, `/operations`, `/audit`, `/me` + admin).
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.127` (expected until this release lands).
- Second audit loop (same checklist): no further fixable defects after the version, copy, pill-contrast, and sitemap updates.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `yp="0.17.143"`).
- The demo-band **version pill** is `text-xs` too — opaque `bg-surface text-brand-strong`, never `bg-surface/20 text-brand-text`.
- New clinician surfaces since v0.17.127 that still do **not** get their own tile: spôsob poučenia § 6 ods. 2, negatívny reverz, súhlasy na HD/PD/CVK/AVF/transfúziu/biopsiu, odstúpenie poskytovateľa § 12 ods. 16, zánik dohody § 12 ods. 18, DPN § 12a ods. 5/8/17, listinný predpis ŠÚKL / eRecept § 79 ods. 1 písm. r, ožiarenia § 44a/§ 44d, doklad RÚVZ § 4 vyhl. 585/2008, telekonzultácia totožnosť § 8 ods. 13, správa pre VLD § 8 ods. 6, vyhl. 127/2014, preddialyzačný skríning, § 2a ods. 2 lehota 20 dní, § 79b, § 9d, § 23 ods. 9.
- `next@16.3.5` is still inside the feature-vetting window (re-evaluate 2026-09-18 or on a GHSA).

### Run 23 — 2026-09-18 (Demo alignment v0.17.157, Next.js 16.3.5)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 2 (fixed)
- Low: 1 (fixed)
- Info: 1 remaining (homepage client bundle size; not actionable this run)

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`, `AGENTS.md`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-17 19:08 UTC (`index-DDhbrCEL.js`, HTML `Last-Modified: Thu, 17 Sep 2026 19:08:10 GMT`). The bundle constant is **Arenibus v0.17.157** (`bp="0.17.157"`). Marketing badges and JSON-LD still said **v0.17.143** from Run 22.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** `DEMO_VERSION` is now `v0.17.157`; JSON-LD `softwareVersion` follows `DEMO_VERSION_PLAIN`.

##### 2. Feature-card copy lagged clinician-facing work shipped between v0.17.143 and v0.17.157, and mislabelled § 79 ods. 4
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Clinician nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit). New first-class nav item **Prístroje** (`/equipment`) is technik+admin only (`ws = admin + technik`) — fold into HD, do not add a tile. New/corrected clinician work: oznámenia podozrenia § 79 ods. 4 are týranie / zanedbávanie / zneužívanie / znásilnenie / sexuálne násilie / súlož medzi príbuznými (OČTK, obec, UPSVaR), not infectious-disease reports; paliatívne náležitosti (sociálna pomoc, duchovná služba, nedostupnosť v lehote); poučená osoba § 6 ods. 1; poučenie o rozsahu a zmluve § 79 ods. 1 písm. h; oznámenie očkovania § 13 ods. 5; oznámenie VLD pri osobnej starostlivosti; čakacia listina zák. 317/2016; biomedicínsky výskum § 26 ods. 11; žiadosti o nápravu § 17; uchovávanie dokumentácie; miesto poskytnutia § 8 ods. 1; domáca na žiadosť § 8 ods. 10 a doplnkové hodiny § 2a ods. 3 (uhrádza osoba, nevykazuje sa poisťovni); HD sedenia s napojením/ukončením/zrušením a § 21 ods. 1 a 2; cievny prístup prvé použitie / znovuotvorenie; žiadanka SVLZ sestrou § 19a ods. 1; súvisiace služby § 13 písm. c/e/f; písomná forma § 20 ods. 2 (výpadok IS/NZIS, zákon, záznamy nad rámec EZK); očkovanie pracovníkov § 5 ods. 1 vyhl. 585/2008; incidenty § 9b ods. 2; obsadenosť pozícií; ordinačné hodiny § 79 ods. 1 písm. z; oznamy o zastupovaní/pozastavení/skončení § 79 ods. 1 písm. j) a k); ePZP stavy stratený/odcudzený/zničený/nahradený.
- **Recommendation:** Keep the ten-card layout; fold Prístroje into Hemodialýza. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Hemodialýza, Laboratórium, Moje zoznamy, Vykazovanie, and Prevádzka. PD, Kalendár, and Audit copy remain accurate.

##### 3. Next.js 16.3.5 vetting window elapsed
- **Severity:** Medium
- **Category:** Security / Code Quality
- **File(s):** `package.json`, `package-lock.json`
- **Description:** Latest stable is `next@16.3.5` (published 2026-09-11). The 7-day feature window closed 2026-09-18. Changelog backports image-cache empty-file guards, standalone NFT emission, CSP nonces on loading/template scripts, and a `use cache` prerender fix — none of which change this Linux static export with `images.unoptimized: true`. No GHSA / critical advisory (August 2026 RCEs remain patched since 16.3.3). 16.4 is still canary-only.
- **Recommendation:** Adopt 16.3.5 now that the window has elapsed; keep `images.unoptimized: true`.
- **Fixed:** Upgraded `next` and `eslint-config-next` to exact `16.3.5`. `npm audit` reports 0 vulnerabilities.

##### 4. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Description:** All three `<loc>` entries still listed 2026-09-17 after a new release day.
- **Fixed:** All three now `2026-09-18`.

##### 5. Largest JS chunk remains ~229 KB uncompressed
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/3kmozkq6tgqnz.js` is 228 922 bytes — same homepage client-bundle baseline as Run 22. Copy and Next.js patch do not add a client library.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 44 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.5 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.157` twice on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.157`; `<header>` + `<main id="main-content">` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed` on all three routes; visible contact labels; demo version pill is opaque `surface`.
- Assets: `og-image.png` 1200×630 (139 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-DDhbrCEL.js` constant `bp="0.17.157"`; lazy chunks share Last-Modified `Thu, 17 Sep 2026 19:08:10 GMT`; clinician nav unchanged; `/equipment` (Prístroje) is technik+admin.
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with prior `v0.17.143` (expected until this release lands).
- Second audit loop (same checklist): no further fixable defects after the version, copy, Next.js, and sitemap updates.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; the version string inside the new file is the source of truth (this run the constant is `bp="0.17.157"`).
- **Prístroje** (`/equipment`) is a dedicated nav item for `admin` + `technik` only. Fold it into Hemodialýza; do not add an eleventh tile. Same rule as Cenník / Monitory.
- § 79 ods. 4 on the patient card is **oznámenia podozrenia z týrania a zneužívania** (OČTK / obec / UPSVaR), not infectious-disease reporting (that stays § 4 vyhl. 585/2008 / § 11 ods. 14).
- `next@16.3.5` is now the production baseline. Re-evaluate only when a newer 16.3.x / 16.4 stable (or a critical security patch) appears.

### Run 24 — 2026-09-19 (Demo unchanged v0.17.157; server homepage + SEO/a11y hygiene)

#### Summary
- Critical: 0
- High: 0
- Medium: 1 (fixed)
- Low: 3 (fixed)
- Info: 2 remaining (shared Next/React chunk size; React 19.3 deferred)

#### Findings

##### 1. Live demo is still v0.17.157 — no new clinician tiles
- **Severity:** Info
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`
- **Description:** https://demo.arenibus.com/ HTML `Last-Modified: Thu, 17 Sep 2026 19:08:10 GMT`, bundle still `index-DDhbrCEL.js` (`bp="0.17.157"`, 277 104 bytes). Clinician nav unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit. **Prístroje** (`/equipment`) remains technik+admin. Marketing badges and JSON-LD already said **v0.17.157**.
- **Recommendation:** Do not invent tiles when the HTML Last-Modified and bundle hash are unchanged. Spend the run on WCAG / SEO / dependency work.
- **Fixed:** Version constant left at `v0.17.157`. Feature-card copy left aligned with Run 23.

##### 2. Entire homepage was a single `"use client"` module
- **Severity:** Medium
- **Category:** Performance / Code Quality
- **File(s):** `app/page.tsx`, `app/site-header.tsx`, `app/contact-form.tsx`, `scripts/check-links.mjs`
- **Description:** Hero, ten feature cards, demo band and footer hydrated as one client tree only because the mobile menu and Formspree form need state. The 229 KB `3kmozkq6tgqnz.js` chunk is the shared Next/React runtime (unchanged name and size since Run 23), not the feature copy.
- **Recommendation:** Keep `app/page.tsx` as a Server Component. Isolate interactivity in `SiteHeader` and `ContactForm`. Teach `check:links` to resolve `#fragments` declared on an island against the importing `page.tsx`.
- **Fixed:** Homepage is a server component; skip link stays inline. `check:links` now walks importing route pages for hash targets.

##### 3. JSON-LD omitted the implemented module list
- **Severity:** Low
- **Category:** SEO
- **File(s):** `app/layout.tsx`
- **Description:** `SoftwareApplication` had `softwareVersion` but no `featureList`, so crawlers could not see the ten clinician modules as structured data.
- **Fixed:** Added `featureList` for the ten cards (Pacienti through Audit). Admin-only screens stay folded.

##### 4. Body font named a webfont that is never loaded
- **Severity:** Low
- **Category:** Performance
- **File(s):** `app/globals.css`
- **Description:** `font-family` started with `"Inter Variable"` but no `@font-face` or Google Fonts link exists (parent CSP would also block fonts.googleapis.com from this subdirectory unless already allowed — it is allowed for the parent, but this site does not request Inter).
- **Fixed:** System stack only: `"Segoe UI", system-ui, -apple-system, "Helvetica Neue", sans-serif`.

##### 5. Contrast checker missed legal-page `muted` on `surface`
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `scripts/check-contrast.mjs`
- **Description:** `/privacy/` and `/terms/` render the effective-date line as `text-muted text-sm` on `bg-surface`. The pair already meets AA (7.29:1 light / 6.22:1 dark) but was not in `PAIRS`.
- **Fixed:** Added `legal effective-date (text-sm)`.

##### 6. Deploy workflow had no explicit GitHub token permissions
- **Severity:** Low
- **Category:** Security
- **File(s):** `.github/workflows/deploy.yml`
- **Description:** The job only needs the checkout. Default `GITHUB_TOKEN` write is unnecessary.
- **Fixed:** `permissions: contents: read`.

##### 7. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Fixed:** All three `<loc>` entries are now `2026-09-19`.

##### 8. Next.js / React remain at the vetted baseline
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable Next.js is still `16.3.5` (npm `modified` 2026-09-18 23:55 UTC). 16.4 remains canary-only (`16.4.0-canary.36`). No new GHSA beyond the August 2026 RCEs already patched in 16.3.3+. `react@19.3.0` (2026-09-09) is past the 7-day window but Next.js 16.3.5 is tested with `19.2.4`; do not upgrade React independently. `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- **Recommendation:** Re-evaluate on a newer 16.3.x / 16.4 stable, a Next.js release that lists React 19.3, or a critical advisory. Do not use `npm audit fix --force`.

##### 9. Largest JS chunk remains the shared Next/React runtime (~229 KB)
- **Severity:** Info
- **Category:** Performance
- **Description:** `out/_next/static/chunks/3kmozkq6tgqnz.js` is still 228 922 bytes. Homepage-specific client islands compile to a separate ~31 KB chunk. Further cuts need a framework upgrade or dropping `next/image` in the header, not more copy edits.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 46 token pairs pass WCAG 2.1 AA in both themes (added legal `muted` on `surface`); dark-palette parity holds.
- `npm run build`: Next.js 16.3.5 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments (including header islands → `app/page.tsx`), PHP redirects, and sitemap locs pass locally (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex`; exactly one `<h1>` per route; version `v0.17.157` on the homepage (hero + demo band); JSON-LD `softwareVersion` `0.17.157` and `featureList` present; `<header>` + `<main id="main-content">` + skip link on `/`, `/privacy/`, `/terms/`; `#mobile-menu` present when closed; no `text-white`; theme toggle emits `aria-pressed` on all three routes; visible contact labels; demo version pill is opaque `surface`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget.
- Demo cross-check: bundle `index-DDhbrCEL.js` constant `0.17.157`; HTML and JS Last-Modified `Thu, 17 Sep 2026 19:08:10 GMT`; clinician nav unchanged; `/equipment` (Prístroje) is technik+admin.
- Live production before deploy: `https://arenibus.polascin.net/` HTTP 200 with `v0.17.157` (same demo version; this release is hygiene + architecture).
- Second audit loop (same checklist): no further fixable defects after the island split, JSON-LD, font, contrast-pair, workflow, and sitemap updates.

#### Self-improvement notes
- When demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, do not invent feature tiles. Confirm the unique `X.Y.Z` in the bundle, then spend the run on WCAG / SEO / dependencies.
- The ~229 KB `3kmozkq6tgqnz.js` (name may change) is the **shared Next/React runtime**, not the feature-card copy. Do not treat it as a homepage-content regression.
- Homepage must stay a Server Component. Skip link stays inline on each page. Hash links in `app/site-header.tsx` are allowed — `check:links` resolves them via the importing `page.tsx`.
- Do not put `"Inter Variable"` (or any unloaded webfont) first in `body` `font-family`.
- JSON-LD `featureList` must list the ten clinician modules, not admin-only screens.
- `react@19.3.x` waits for a Next.js release that ships or documents it, even after the 7-day window.

### Run 25 — 2026-09-20 (Demo unchanged v0.17.157; 404, form fallback, SEO/a11y hygiene)

#### Summary
- Critical: 0
- High: 0
- Medium: 2 (fixed)
- Low: 5 (fixed)
- Info: 2 remaining (shared Next/React chunk size; React 19.3 deferred). Parent-host CSP still blocks Formspree `connect-src` / `form-action` — mitigated by mailto fallback, not removable from this repo.

#### Findings

##### 1. Live demo is still v0.17.157 — no new clinician tiles
- **Severity:** Info
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`
- **Description:** https://demo.arenibus.com/ HTML `Last-Modified: Thu, 17 Sep 2026 19:08:10 GMT`, bundle still `index-DDhbrCEL.js` (`0.17.157`, 277 820 bytes). Clinician nav unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit. **Prístroje** (`/equipment`) remains technik+admin. Marketing badges and JSON-LD already said **v0.17.157**.
- **Recommendation:** Do not invent tiles when the HTML Last-Modified and bundle hash are unchanged. Spend the run on WCAG / SEO / dependency work.
- **Fixed:** Version constant left at `v0.17.157`. Feature-card copy left aligned with Run 23/24.

##### 2. Contact form failed closed when Formspree was blocked
- **Severity:** Medium
- **Category:** Accessibility / Security
- **File(s):** `app/contact-form.tsx`
- **Description:** Parent OpenResty CSP still has `connect-src` without `formspree.io` and `form-action 'self'`. A native POST to Formspree would be blocked; `fetch` fails in the browser. The previous error copy mentioned e-mail only as prose. No-JS users had no in-form path.
- **Recommendation:** Keep trying Formspree (works if the parent CSP is later relaxed). On failure, expose a real `mailto:arenibus@polascin.net` with a prefilled subject/body. Add `<noscript>` with the same address. Do not set `action="https://formspree.io/..."` — `form-action 'self'` would block it.
- **Fixed:** Mailto fallback + noscript + `maxLength` + Formspree `_gotcha` honeypot. Visible error link stays a static `mailto:` so `check:links` can resolve it.

##### 3. Missing branded 404
- **Severity:** Medium
- **Category:** SEO / Accessibility
- **File(s):** `app/not-found.tsx`
- **Description:** Static export only had the default Next.js not-found page. A broken inbound link showed no skip link, theme toggle, or Slovak chrome.
- **Fixed:** `app/not-found.tsx` with inline skip link, sticky nav, theme toggle, and shared footer. Next.js still emits `noindex` on `/_not-found/` (correct).

##### 4. Legal pages had no breadcrumbs or `aria-current`
- **Severity:** Low
- **Category:** SEO / Accessibility
- **File(s):** `app/breadcrumbs.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`, `app/site-footer.tsx`
- **Description:** `/privacy/` and `/terms/` had no visible path back to the homepage besides the nav label, no `BreadcrumbList` JSON-LD, and footer links did not mark the current page.
- **Fixed:** Visible breadcrumbs, `BreadcrumbList` JSON-LD, shared `SiteFooter` with `aria-current="page"`.

##### 5. Theme toggle ignored OS preference changes
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/theme-toggle.tsx`
- **Description:** `useSyncExternalStore` only subscribed to `storage` events. With no saved `localStorage` value, a system theme change did not update `aria-pressed` or `data-theme`.
- **Fixed:** Also subscribe to `matchMedia("(prefers-color-scheme: dark)")` when no stored preference exists.

##### 6. Homepage brand mark was not a link; mobile menu icon did not reflect open state
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/site-header.tsx`
- **Fixed:** Logo + name now link to `/`. The menu SVG switches to an X while `aria-expanded` is true.

##### 7. Missing `security.txt`, telephone auto-link, and WebSite JSON-LD
- **Severity:** Low
- **Category:** SEO / Security
- **File(s):** `public/.well-known/security.txt`, `app/layout.tsx`, `app/privacy/page.tsx`
- **Description:** No RFC 9116 contact file. IČO `57646856` can be auto-linked as a phone number on iOS. JSON-LD had `SoftwareApplication` but no `WebSite` node and no `installUrl` for the demo.
- **Fixed:** `security.txt` (expires 2027-03-20, linked from the privacy page), `formatDetection.telephone: false`, `applicationName`, `WebSite` JSON-LD, `installUrl` on the software node. Legal OG image alts now match the homepage.

##### 8. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Fixed:** All three `<loc>` entries are now `2026-09-20`.

##### 9. Link/contrast checkers lagged the new markup
- **Severity:** Low
- **Category:** Code Quality
- **File(s):** `scripts/check-links.mjs`, `scripts/check-contrast.mjs`
- **Description:** `fetch(FORMSPREE_ENDPOINT)` was not resolved; `<form action>` was not inventoried. New pairs (breadcrumb, 404 status, form fallback link on `surface-2`) were missing.
- **Fixed:** Checker now resolves fetch identifiers and form actions. Contrast table has 50 pairs.

##### 10. Next.js / React remain at the vetted baseline
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable Next.js is still `16.3.5`. 16.4 remains canary-only (`16.4.0-canary.37`). No new GHSA. `react@19.3.0` stays deferred. `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- **Recommendation:** Re-evaluate on a newer 16.3.x / 16.4 stable, a Next.js release that lists React 19.3, or a critical advisory. Do not use `npm audit fix --force`. Parent CSP should add `formspree.io` to `connect-src` and `form-action` if the hosted form should succeed without mailto.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 50 token pairs pass WCAG 2.1 AA in both themes; dark-palette parity holds.
- `npm run build`: Next.js 16.3.5 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, sitemap locs, `/.well-known/security.txt`, and Formspree fetch pass (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex` on indexable routes (`/_not-found/` correctly has `noindex`); exactly one `<h1>` per route; version `v0.17.157` on the homepage; JSON-LD `softwareVersion` `0.17.157`, `featureList`, `WebSite`, and legal `BreadcrumbList`; skip link + `<main id="main-content">` on `/`, `/privacy/`, `/terms/`, `/_not-found/`; theme toggle `aria-pressed`; visible contact labels; `format-detection` telephone=no; `application-name`.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget. `out/.well-known/security.txt` present.
- Demo cross-check: bundle `index-DDhbrCEL.js` constant `0.17.157`; HTML and JS Last-Modified `Thu, 17 Sep 2026 19:08:10 GMT`; clinician nav unchanged; `/equipment` (Prístroje) is technik+admin.
- Live production after first deploy: homepage / privacy / terms / robots / sitemap HTTP 200 with the new copy. `/.well-known/security.txt` and unknown paths 404 at the parent OpenResty layer (iso-8859-1). Follow-up commit moved the user-facing security contact to `/security.txt`.
- Second audit loop (same checklist): added the remaining contrast pairs for the form fallback link; fixed the production-404 on the security.txt link.

#### Self-improvement notes
- When demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, do not invent feature tiles.
- Parent CSP `form-action 'self'` and `connect-src` without `formspree.io` mean the contact form must keep a `mailto:` fallback. Never add a Formspree `action` attribute.
- `app/not-found.tsx` needs the same inline skip link rule as the three public routes.
- `check:links` must resolve `fetch(IDENT)` via file-level `const` strings and inventory `<form action>`.
- Keep `public/security.txt` (and a copy under `public/.well-known/`) with an `Expires` date less than one year out. The privacy-page and footer link must be `/security/` — WebSupport OpenResty 404s `/.well-known/` and WAF 403s `/security.txt`. Set `Canonical` in the text file to the HTML `/security/` page. Parent ErrorDocument is also not our `/_not-found/` page.
- Shared chrome (footer) is fine; skip links stay inline on each page.

### Run 26 — 2026-09-21 (Demo alignment v0.17.158 & repeated audit)

#### Summary
- Critical: 0
- High: 1 (fixed)
- Medium: 1 (fixed)
- Low: 2 (fixed)
- Info: 2 remaining (shared Next/React chunk size; React 19.3 deferred). Parent-host CSP still blocks Formspree `connect-src` / `form-action` — mitigated by mailto fallback, not removable from this repo.

#### Findings

##### 1. Marketing site advertised a stale demo version
- **Severity:** High
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`
- **Description:** Live demo at https://demo.arenibus.com/ was redeployed 2026-09-20 16:39 UTC (`index-1Qp_Ylco.js`, HTML `Last-Modified: Sun, 20 Sep 2026 16:39:23 GMT`, 277 747 bytes). The bundle constant is **Arenibus v0.17.158** (`Sp="0.17.158"`). Marketing badges and JSON-LD still said **v0.17.157** from Run 23–25.
- **Recommendation:** Refresh the single `DEMO_VERSION` constant whenever the demo bundle version changes.
- **Fixed:** `DEMO_VERSION` is now `v0.17.158`; JSON-LD `softwareVersion` follows `DEMO_VERSION_PLAIN`.

##### 2. Feature-card copy lagged clinician-facing work shipped in v0.17.158
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** Clinician nav is unchanged (Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit). **Prístroje** (`/equipment`) remains technik+admin. New lazy page **PatientIdentificationPage** is not a tile: identifikačná karta (meno, poisťovňa, adresa a kontakt) for roles without clinical access. Other clinician work now visible in the bundle: overenie poisťovne v registri poistencov ÚDZS pri registrácii; dohoda o ZS mimo registra NCZI (§ 12 ods. 2/3) s písomným upovedomením o odstúpení (§ 12 ods. 17) a dôvodmi poskytovateľa (§ 12 ods. 9/11); PN liečebný režim § 12a ods. 3, zrušenie vychádzok § 12a ods. 7, pokračovanie § 12a ods. 10, nedostavenie sa § 12a ods. 6; osobná starostlivosť dôvod § 12b ods. 2 a hospitalizácia ktorá potrebu neukončuje § 12b ods. 9; posudok so súhlasom § 16 ods. 3; výpis § 24 ods. 3/4; zbavenie mlčanlivosti § 80 ods. 3 až 6; biomedicínsky výskum § 26 ods. 5–9; potvrdenie ambulantnej starostlivosti § 19 ods. 2 písm. f; dôvod nevyžadovania odporúčania § 8 ods. 5; žiadanka SVLZ § 19a ods. 2/3 a § 19 ods. 2 písm. c; indikátory kvality § 79 ods. 1 písm. t / § 79a ods. 1 písm. b; podklady kontroly § 79 ods. 1 písm. q / § 79a ods. 1 písm. d; doklad o výške úhrady § 79 ods. 1 písm. o/g; žiadosť o potvrdenie neodkladnej starostlivosti § 79a ods. 1 písm. a; oznámenia s kódom náhrady k poslednému dňu mesiaca § 79a ods. 1 písm. c; kvalifikácie presne podľa dokladu § 79 ods. 15 / § 83c ods. 8 / § 83d ods. 1; NRZP § 79 ods. 1 písm. zm; oznámenie kraju § 79 ods. 18; záložné kópie § 20 ods. 5; opatrenia § 9b ods. 3; poistenie § 79 ods. 1 písm. s; prehliadky mŕtvych § 79 ods. 1 písm. w; zmeny v povolení § 79 ods. 11.
- **Recommendation:** Keep the ten-card layout. Fold PatientIdentificationPage and ÚDZS register lookup into Správa Pacientov. Do not add tiles for Môj profil, Monitory, or admin-only screens.
- **Fixed:** Updated Správa Pacientov, Evidencia Návštev, Laboratórium, Moje zoznamy, Vykazovanie, and Prevádzka. HD, PD, Kalendár, and Audit copy remain accurate.

##### 3. Footer omitted the security contact; `security.txt` Canonical 403s
- **Severity:** Low
- **Category:** SEO / Accessibility
- **File(s):** `app/site-footer.tsx`, `app/security/page.tsx`, `public/security.txt`, `public/.well-known/security.txt`
- **Description:** `/security/` existed but was only linked from the privacy page. Footer had no `aria-current` on that route. RFC 9116 `Canonical` pointed at `/security.txt`, which the parent WAF 403s.
- **Fixed:** Footer now links `/security/` with `aria-current="page"` on that route. Both `security.txt` copies set `Canonical` to `https://arenibus.polascin.net/security/`.

##### 4. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Fixed:** All four `<loc>` entries are now `2026-09-21`.

##### 5. Next.js / React remain at the vetted baseline
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable Next.js is still `16.3.5`. 16.4 remains canary-only. No new GHSA. `react@19.3.0` stays deferred. `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- **Recommendation:** Re-evaluate on a newer 16.3.x / 16.4 stable, a Next.js release that lists React 19.3, or a critical advisory. Do not use `npm audit fix --force`.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 27 token pairs (×2 themes) pass WCAG 2.1 AA; dark-palette parity holds.
- `npm run build`: Next.js 16.3.5 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/security`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, sitemap locs, `/security/`, and Formspree fetch pass (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; security 31 / 104; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex` on indexable routes (`/_not-found/` correctly has `noindex`); exactly one `<h1>` per route; version `v0.17.158` on the homepage; JSON-LD `softwareVersion` `0.17.158`; skip link + `<main id="main-content">` on `/`, `/privacy/`, `/terms/`, `/security/`, `/_not-found/`.
- Assets: `og-image.png` 1200×630 (139 KB); apple-touch 180×180; icons 192 and 512; logos within budget. Largest JS chunk `3kmozkq6tgqnz.js` remains 228 922 bytes (shared Next/React runtime).
- Demo cross-check: bundle `index-1Qp_Ylco.js` constant `Sp="0.17.158"`; HTML and JS Last-Modified `Sun, 20 Sep 2026 16:39:23 GMT`; clinician nav unchanged; `/equipment` (Prístroje) is technik+admin; new `PatientIdentificationPage-tQ5T0A6n.js`.

#### Self-improvement notes
- After a demo redeploy the HTML `Last-Modified` and the **JS bundle hash** (`/assets/index-*.js`) change together; this run the constant is `Sp="0.17.158"`.
- A new lazy page is not automatically a new tile. `PatientIdentificationPage` is a restricted identification view — fold it into Správa Pacientov.
- Footer legal links should include `/security/` with `aria-current` on that route. `security.txt` `Canonical` must be the reachable HTML page, not the WAF-blocked `/security.txt`.
- When demo HTML `Last-Modified` and `/assets/index-*.js` hash are unchanged, do not invent feature tiles.

### Run 27 — 2026-09-22 (Demo unchanged v0.17.158; copy, a11y, deploy smoke)

#### Summary
- Critical: 0
- High: 0
- Medium: 1 (fixed)
- Low: 4 (fixed)
- Info: 2 remaining (shared Next/React chunk size; React 19.3 deferred). Parent-host CSP still blocks Formspree `connect-src` / `form-action` — mitigated by mailto fallback, not removable from this repo.

#### Findings

##### 1. Live demo is still v0.17.158 — no new clinician tiles
- **Severity:** Info
- **Category:** Content
- **File(s):** `lib/site.ts`, `app/page.tsx`
- **Description:** https://demo.arenibus.com/ HTML `Last-Modified: Sun, 20 Sep 2026 16:39:23 GMT`, bundle still `index-1Qp_Ylco.js` (`Sp="0.17.158"`, 277 747 bytes). Clinician nav unchanged: Pacienti, Kalendár, HD, PD, Moje zoznamy, Štatistiky, Vykazovanie, Prevádzka, Audit. **Prístroje** (`/equipment`) remains technik+admin. Marketing badges and JSON-LD already said **v0.17.158**.
- **Recommendation:** Do not invent tiles when the HTML Last-Modified and bundle hash are unchanged. Still parse lazy page chunks for copy that Run 26 missed.
- **Fixed:** Version constant left at `v0.17.158`. Ten-card layout unchanged.

##### 2. Feature-card copy still lagged implemented work already in v0.17.158
- **Severity:** Medium
- **Category:** Content
- **File(s):** `app/page.tsx`, `AGENTS.md`
- **Description:** The same bundle already contains clinician-facing fields that the marketing cards omitted: krvná skupina on the identification/registration card; DPN posudzovaná aj lekárom špecializovanej ambulantnej starostlivosti (§ 12a ods. 1 písm. c); upovedomenie VLD pri osobnej starostlivosti (§ 12b ods. 3 písm. c); nahliadnutie inšpektora správnej klinickej praxe (§ 25 ods. 1 písm. t, len pseudonymizované kópie); antropometria v zápise návštevy (BMI podľa WHO, BSA Mosteller/DuBois, viscerálny tuk, suchá hmotnosť, IBW, LBW); karta prístroja (§ 79 ods. 1 písm. d); sústavné vzdelávanie s evidenciou aktivít a hodnotení komory (§ 42, § 42 ods. 7, § 80 ods. 1 písm. b); zmluvy na viditeľnom mieste (§ 79 ods. 1 písm. f); písomne dokumentovaný systém kvality (§ 9 ods. 1 a 2); odovzdanie dokumentácie jednotlivému pacientovi (§ 23 ods. 3). Demo itself still limits oznámenia poisťovni to kódy náhrady **01 až 04 alebo 06** — do not advertise 05/07/08 as notifiable.
- **Recommendation:** Keep the ten-card layout. Fold these into the existing Pacienti / Návštevy / HD / Prevádzka / Štatistiky cards.
- **Fixed:** Updated those five cards. PD, Kalendár, Laboratórium, Vykazovanie, and Audit copy remain accurate.

##### 3. In-page hash links landed under the sticky header
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/globals.css`
- **Description:** `html` has `scroll-smooth` but no `scroll-padding-top`. The sticky nav is `h-16` (4rem). `#features`, `#demo`, `#contact`, and `#main-content` therefore hide their headings under the header after a keyboard or click jump.
- **Fixed:** `html { scroll-padding-top: 5rem; }`.

##### 4. Deploy smoke test only hit `/` without a browser User-Agent
- **Severity:** Low
- **Category:** Deployment
- **File(s):** `.github/workflows/deploy.yml`, `deploy.sh`
- **Description:** Production WAF can return HTTP 466 on `/robots.txt`, `/sitemap.xml`, `/privacy/`, and `/terms/` for short/bot User-Agents. The job only curled the homepage and would miss a broken legal route.
- **Fixed:** Chrome UA plus `/`, `/privacy/`, `/terms/`, `/security/`, `/robots.txt`, `/sitemap.xml`. Same set in `deploy.sh`.

##### 5. Contact-form honeypot was only CSS-hidden
- **Severity:** Low
- **Category:** Accessibility
- **File(s):** `app/contact-form.tsx`
- **Description:** `_gotcha` used `className="hidden"` and `aria-hidden` but not the HTML `hidden` attribute, so it remained a named form control in the accessibility tree if CSS failed to load.
- **Fixed:** Added the `hidden` attribute (kept `tabIndex={-1}` and `aria-hidden`).

##### 6. Sitemap `<lastmod>` was a day behind this deploy
- **Severity:** Low
- **Category:** SEO
- **File(s):** `public/sitemap.xml`
- **Fixed:** All four `<loc>` entries are now `2026-09-22`.

##### 7. Next.js / React remain at the vetted baseline
- **Severity:** Info
- **Category:** Security / Code Quality
- **Description:** Latest stable Next.js is still `16.3.5`. 16.4 remains canary-only (`16.4.0-canary.37`). No new GHSA. `react@19.3.0` stays deferred. `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- **Recommendation:** Re-evaluate on a newer 16.3.x / 16.4 stable, a Next.js release that lists React 19.3, or a critical advisory. Do not use `npm audit fix --force`.

#### Verification performed
- `npm run lint`: clean.
- `npm run check:contrast`: all 27 token pairs (×2 themes) pass WCAG 2.1 AA; dark-palette parity holds.
- `npm run build`: Next.js 16.3.5 (Turbopack) static export for `/`, `/privacy`, `/terms`, `/security`, `/_not-found`.
- `npm run check:links`: all user-facing HTTP(S), mailto, fragments, PHP redirects, sitemap locs, `/security/`, and Formspree fetch pass (`dataprotection.gov.sk` → 200; Formspree GET → 405).
- `npm audit` / `npm audit --omit=dev`: 0 vulnerabilities.
- Secret scan: no hardcoded credentials (demo passwords remain on-request only).
- Generated HTML: homepage title 54 / description 137; privacy 41 / 150; terms 31 / 121; security 31 / 104; `lang="sk"`; canonicals absolute; `og:image` absolute; no `noindex` on indexable routes (`/_not-found/` correctly has `noindex`); exactly one `<h1>` per route; version `v0.17.158` four times on the homepage; JSON-LD `softwareVersion` `0.17.158`; skip link + `<main id="main-content">` on `/`, `/privacy/`, `/terms/`, `/security/`, `/_not-found/`; new copy (`krvná skupina`, antropometria, § 42, § 23 ods. 3) present; `scroll-padding-top` in the exported CSS.
- Assets: `og-image.png` 1200×630 (142 KB); apple-touch 180×180; icons 192 and 512; logos within budget. Largest JS chunk `3kmozkq6tgqnz.js` remains 228 922 bytes (shared Next/React runtime).
- Demo cross-check: bundle `index-1Qp_Ylco.js` constant `Sp="0.17.158"`; HTML and JS Last-Modified `Sun, 20 Sep 2026 16:39:23 GMT`; clinician nav unchanged; `/equipment` (Prístroje) is technik+admin.
- Second audit loop (same checklist): no further fixable defects after the copy, scroll-padding, honeypot, sitemap, and smoke-test updates.

#### Self-improvement notes
- An unchanged demo **bundle hash** still warrants reading lazy page chunks (`PatientDetailPage`, `OperationsPage`, `BillingPage`, …). Version and tiles stay put; card *copy* can lag fields already shipped.
- Do not treat extra náhrada codes 05/07/08 as notifiable — the billing UI itself still says **01 až 04 alebo 06**.
- `html` must keep `scroll-padding-top` (currently 5rem) so in-page hashes clear the sticky `h-16` nav.
- CI smoke test must use a full Chrome UA and check `/`, `/privacy/`, `/terms/`, `/security/`, `/robots.txt`, and `/sitemap.xml`.

---

## 📝 Known Project-Specific Checks (fill during runs)

- Hero heading must remain `Nefrologický a dialyzačný informačný systém`, with the MVP version badge and the advanced-development notice nearby.
- `DEMO_VERSION` in `lib/site.ts` (imported by `app/page.tsx` and JSON-LD in `app/layout.tsx`) must match the version shown on https://demo.arenibus.com/ (footer `Arenibus vX.Y.Z`). Do not leave the marketing badges or `softwareVersion` on a previous patch.
- Feature cards must cover every primary demo nav module that clinicians see: Pacienti, Evidencia návštev, Hemodialýza, Peritoneálna dialýza, Kalendár, Laboratórium, Moje zoznamy / Štatistiky, Vykazovanie, Prevádzka, Audit. Admin-only Poskytovateľ, Používatelia, Monitory, Cenník and Uchovávanie, plus user `/me` (Môj profil) and technik+admin **Prístroje** (`/equipment`), may be folded into those cards. `PatientIdentificationPage` is not a tile — fold the identifikačná karta (meno, poisťovňa, adresa, kontakt a krvná skupina) into Správa Pacientov. Card copy must also track patient-card sections (DPN § 12a ods. 1 písm. c, osobná starostlivosť § 12b ods. 3 písm. c, nahliadnutie inšpektora SKP § 25 ods. 1 písm. t), encounter antropometria, karta prístroja § 79 ods. 1 písm. d, vzdelávanie § 42 / § 80 ods. 1 písm. b, zmluvy § 79 ods. 1 písm. f, systém kvality § 9 ods. 1 a 2, and odovzdanie pacientovi § 23 ods. 3. Do not conflate § 79 ods. 4 (týranie/zneužívanie) with hlásenia prenosných ochorení. Oznámenia poisťovni stay **01 až 04 alebo 06**.
- Features section heading must read: `Funkcie nefrologického a dialyzačného informačného systému Arenibus v aktuálnej verzii` with `(MVP = Minimum Viable Product)` and Slovak translation `(Minimálny životaschopný produkt)` on separate lines.
- Brand color must remain teal/petrol: `#0f766e` (light), `#2dd4bf` (dark mode brand).
- Dark mode toggle must persist preference in `localStorage` and respect system preference on first load. Implemented via `useSyncExternalStore` in `app/theme-toggle.tsx` to avoid `setState` in effect lint errors. Subscribe to `matchMedia` as well when no stored preference exists. Mount the same control on every sticky nav (`/`, `/privacy/`, `/terms/`, and `app/not-found.tsx`).
- Demo CTA button text must be clearly visible in both light and dark modes. Never use `text-white` / `hover:text-white` on the demo brand band; use `text-brand-text` (dark `--brand` `#2dd4bf` fails AA against white).
- Small text on the demo band must sit on an opaque `surface` / `surface-2` card, including the `text-xs` version pill (`bg-surface text-brand-strong`). Do not reintroduce `bg-surface/10` (or `/20`) overlays with `text-brand-text/90` or nested `opacity-75` — composited contrast fails WCAG AA in light mode and the solid-token checker will not catch it.
- Homepage `app/page.tsx` is a Server Component. Mobile nav lives in `app/site-header.tsx` and the Formspree form in `app/contact-form.tsx`. Do not put `"use client"` back on the homepage.
- JSON-LD `SoftwareApplication` must include `featureList` for the ten clinician modules (not admin-only screens) and `installUrl` pointing at the live demo. A second `WebSite` node belongs in `<head>`. Legal pages must also emit `BreadcrumbList`.
- Do not put `"Inter Variable"` or another unloaded webfont first in `body` `font-family`.
- Feature cards must use `h3` (not `h4`) to maintain correct heading hierarchy under the section `h2`.
- Contact form must have a **visible** `<label>` for every input (not only `sr-only`). Name and e-mail fields must also set `autoComplete="name"` and `autoComplete="email"` (WCAG 1.3.5 / 3.3.2). Keep a `mailto:arenibus@polascin.net` fallback (and `<noscript>`) because parent CSP may block Formspree. Do not set a Formspree `action` attribute.
- Root metadata must set `formatDetection.telephone: false` so the footer IČO is not auto-linked as a phone number.
- Every route with a sticky nav (`/`, `/privacy/`, `/terms/`, and `app/not-found.tsx`) must offer an inline skip link „Preskočiť na hlavný obsah“ targeting `#main-content` on that same page. Put that `id` on `<main>` (not a hero `<section>`), wrap the sticky nav in `<header>`, and keep a `<nav aria-label="Hlavná navigácia">`. Do not extract the skip link into a shared component — `npm run check:links` resolves fragments against the file that declares the `href`. Shared footer chrome (`app/site-footer.tsx`) is allowed. `html` must set `scroll-padding-top` (currently 5rem) so `#features`, `#demo`, `#contact`, and `#main-content` clear the sticky `h-16` nav.
- Dark-mode toggle must keep `aria-pressed` (boolean) in addition to `aria-label="Prepnúť tmavý režim"`, on every route that has a sticky nav.
- Mobile menu must support `Escape` key to close. The `#mobile-menu` node must stay in the DOM whenever the toggle declares `aria-controls="mobile-menu"` (toggle with `hidden` / CSS, do not conditional-mount).
- All decorative SVG icons must use `aria-hidden="true"`.
- SEO files must exist in `public/`: `robots.txt`, `sitemap.xml`, `manifest.json`. The user-facing security contact is `/security/` (`app/security/page.tsx`), linked from the privacy page. Do not link `/.well-known/security.txt` or `/security.txt` on this host — OpenResty 404s dot-directories and WAF 403s `security.txt`. The branded `app/not-found.tsx` is served at `/_not-found/` but the parent ErrorDocument for unknown paths is not ours.
- JSON-LD `SoftwareApplication` structured data must be present in `<head>` and include `softwareVersion` (plain `X.Y.Z` from `DEMO_VERSION_PLAIN`), `inLanguage: "sk"`, and an absolute `logo` URL.
- When a parent directory contains another lockfile, `next.config.ts` must set `turbopack.root` to the project root so builds do not infer the wrong workspace.
- With `output: 'export'`, `npm start` must serve `out/` through a static file server rather than invoking `next start`.
- Contact-form success and error feedback must use an ARIA live region. Errors use `role="alert"` and `aria-live="assertive"`; success uses `role="status"` / `polite`.
- Meta description must stay ≤ 160 characters on every route; verify generated `out/index.html`, `out/privacy/index.html`, and `out/terms/index.html` (a homepage-only check missed a 165-character terms description).
- All `aria-label` attributes on interactive controls must use Slovak copy (e.g., dark-mode toggle, mobile menu).
- `public/sitemap.xml` `<lastmod>` should be refreshed on each release/deploy **for every `<loc>`**, not only the homepage.
- Demo credentials must never appear in plain text on the marketing site or anywhere in this public repository. The site offers demo access on request via arenibus@nephroctor.com; the actual credentials live only in the private `arenibus-nis` repo and Keycloak (OIDC).
- Marketing page must display a clear notice that the information system is still in advanced development (e.g., a hero badge and demo-section wording).
- Image assets must be audited by **weight and real dimensions**, not just existence: no single asset should exceed ~200 KB, and the declared `og:image` width/height must match the actual file.
- The purpose-built image set must stay in place: `logo-768.webp` (hero), `logo-96.webp` (nav), `og-image.png` (1200x630 social card), `apple-touch-icon.png` (180x180), `icon-192.png` / `icon-512.png` (manifest). Regenerate from the 2048x2048 master kept in git history (`git show c0af678:public/ArenibusLogo.png`) if the branding changes.
- `npm run check:contrast` must pass. It parses `app/globals.css` directly and also asserts the two dark blocks (`[data-theme="dark"]` and `prefers-color-scheme: dark`) stay in sync. Add a row to its `PAIRS` table whenever a new foreground/background combination appears in the markup.
- `npm run check:links` must pass after `npm run build`. It parses `<a>` / `<Link>` `href`, `fetch()` URLs (string literals or file-level `const` identifiers), and `<form action>` from `app/**/*.tsx` (not Open Graph, Twitter Card, canonical, or JSON-LD URLs), PHP `Location` headers, and sitemap `<loc>` entries. Internal paths must exist in `out/` with a trailing slash; fragments must match an `id` on the declaring file **or** on a `page.tsx` that imports that file (homepage header islands); external HTTP(S) URLs must return 2xx/3xx (Formspree GET may return 405); `mailto:` addresses must be `arenibus@polascin.net` or `arenibus@nephroctor.com`; external `<a>` tags must use `target="_blank"` and `rel="noopener noreferrer"`. Broken links found in an audit must be fixed in the same run. Do not maintain a manual URL allowlist — new links are picked up from source. Network-layer failures (timeout / `fetch failed`) on `dataprotection.gov.sk` after retries are a documented CI exception and must log `WARN`, not fail the job; HTTP 404/5xx from that host remains a failure.
- Layout regressions must be confirmed by measuring `documentElement.scrollWidth` against `clientWidth` inside the page at ~360 px, not by eyeballing a headless screenshot.
- Hero media must stay fluid (`w-full max-w-96 aspect-square`); never reintroduce a fixed `w-96`, which exceeds a 360 px viewport minus the section padding.
- `deploy.sh` and `.github/workflows/deploy.yml` must stay in agreement: both need a guarded `cd`, backups written **outside** the wiped directory, and `find`-based cleanup and permission steps. The live smoke test must use a full Chrome User-Agent and check `/`, `/privacy/`, `/terms/`, `/security/`, `/robots.txt`, and `/sitemap.xml` (parent WAF 466s short UAs on several of those paths).
- Deployment backups must land in `$HOME/arenibus-backups/` with retention, never inside the deploy directory.
- `app/favicon.ico` (App Router convention) shadows `public/favicon.ico`; keep only the `app/` copy.
- A blocking `themeInit` script in `<head>` must apply `data-theme` before first paint; the CSS `prefers-color-scheme` block remains the no-JS fallback.
- Footer copyright must use the same year range as the demo (`Arenibus © 2024–2026 Ľubomír Polaščín` as of 2026-09-12).
- When the demo JS minifies away `im="X.Y.Z"` / `ip="X.Y.Z"`, treat a unique `X.Y.Z` version string in `/assets/index-*.js` (cross-checked against the landing HTML `Last-Modified` and bundle filename) as the source of truth. The minified binding name changes between deploys (`im`, `ip`, `fp`, …).
- Local builds on the Windows workstation must use `npx next build --webpack` (WDAC blocks the native SWC binary); leave `npm run build` on Turbopack for CI.

## ⚠️ Known Security Exceptions

- **None open as of 2026-09-22 (Run 27).** `npm audit` reports 0 vulnerabilities on `next@16.3.5` / `eslint-config-next@16.3.5` (transitive `fast-uri` remains 3.1.7 via `serve` → `ajv`; `js-yaml` is 4.3.2 via `eslint` → `@eslint/eslintrc`).
- `https://dataprotection.gov.sk/` is the official ÚOOÚ URL and remains on the privacy page. GitHub Actions IPs are occasionally dropped at the network layer (`fetch failed` / timeout). `scripts/check-links.mjs` retries then logs `WARN` for that host only; HTTP 404/5xx still fail the job. Observed on run `34549407421` (2026-09-11).
- Next.js **feature** releases are adopted only after a 7-day vetting window. **Critical security patches skip that window**. `next@16.3.4` (2026-08-31) was adopted on 2026-09-07 after the window elapsed. `next@16.3.5` (2026-09-11) was adopted on 2026-09-18 after the window elapsed; image-cache / standalone / CSP-nonce / `use cache` backports do not apply to this Linux static export with `images.unoptimized: true`. `react@19.3.0` (2026-09-09) stays deferred until a Next.js release ships or documents it. Re-evaluate on a newer 16.3.x / 16.4 stable or a critical advisory.
- Hosting-layer CSP on arenibus.polascin.net is inherited from the parent OpenResty site (`script-src` / `connect-src` without `formspree.io` or `'unsafe-inline'`; `form-action 'self'`). Static export cannot emit CSP headers; a subdirectory `.htaccess` would not apply (OpenResty, not Apache). Theme init and the Formspree `fetch` therefore depend on the parent CSP. The contact form keeps a `mailto:` fallback for when `fetch` is blocked. Do not add a Formspree `<form action>` and do not add a page-level meta CSP — it cannot relax an HTTP header.
- No hardcoded secrets in source. `grep` matches for `password|secret|api_key|token|private` occur only in `package-lock.json`, `.next/` build artefacts (not committed), `.audit.md` documentation, `AGENTS.md` secret-name references, and `package.json` `private: true`.
- The contact form posts to Formspree (`https://formspree.io/f/mvzebqzj`) over HTTPS. The form ID is a public endpoint identifier, not a credential.
- Static export cannot emit CSP headers. `out/index.html` carries three bare inline scripts — `themeInit` plus two Next.js RSC payload scripts — so any CSP added at the hosting layer needs hashes or `unsafe-inline` regardless of the theme script.

